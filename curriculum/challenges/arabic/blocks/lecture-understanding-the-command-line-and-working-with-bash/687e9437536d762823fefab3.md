---
id: 687e9437536d762823fefab3
title: ما هي بعض الاختصارات التي يمكنك استخدامها في سطر الأوامر لتسريع الإنتاجية؟
challengeType: 19
dashedName: what-are-some-shortcuts-you-can-use-in-the-command-line-to-speed-up-productivity
---

# --description--

دعنا نتعلم عن اختصارات سطر الأوامر التي يمكنك استخدامها لتسريع الإنتاجية.

يمكن لاختصارات الطرفية وسطر الأوامر أن تساعدك على تبسيط سير عملك وزيادة إنتاجيتك إلى أقصى حد. بالنسبة لنظامي Linux وmacOS، اللذين يعودان جذورهما إلى Unix، ستكون العديد من هذه الاختصارات متشابهة. لكن بالنسبة لنظام Windows، ستكون هناك بعض الاختلافات.

الاختصار الأول هو مفتاح <kbd>سهم للأعلى</kbd>. الضغط على هذا المفتاح يسمح لك بالتنقل للخلف عبر سجل الأوامر، أمرًا واحدًا في كل مرة. ثم يمكن استخدام مفتاح <kbd>سهم للأسفل</kbd> للتنقل للأمام عبر سجل الأوامر. هذان المفتاحان يتيحان لك التنقل بسرعة بين الأوامر التي نفذتها سابقًا للعثور على الأمر الذي تحتاج إلى تشغيله مرة أخرى.

تقدم العديد من الصدوع أيضًا ميزة الإكمال التلقائي، التي تقترح أوامر بناءً على ما بدأت بكتابته. يمكن استخدام مفتاح <kbd>Tab</kbd> لملء بقية الاقتراح، مما يملأ سطر الأوامر بسرعة بالصيغة النحوية الكاملة. تختلف طريقة توليد هذه الاقتراحات من صدع لآخر. على سبيل المثال، في zsh تعتمد هذه الاقتراحات على سجل الأوامر الأخير. أما في PowerShell، فتعتمد على الأوامر والمتغيرات والمعلمات المتاحة (مع وزن أقل لمدى استخدامها مؤخرًا).

أحيانًا قد ينتج عن أمر ما الكثير من المخرجات على الطرفية. قد يعيق هذا الضجيج الإضافي سير عملك أو يسبب إزعاجًا. في الطرفيات المبنية على *nix الموجودة في أجهزة Linux وmacOS، يمكنك الضغط على <kbd>Control</kbd> + <kbd>L</kbd> لمسح الشاشة بالكامل والبدء بموجه نظيف وجديد. في PowerShell، يقوم <kbd>Control</kbd> + <kbd>L</kbd> أيضًا بمسح الشاشة افتراضيًا، ويمكنك تشغيل الأمر `cls` لنفس النتيجة.

إذا احتجت إلى مقاطعة أمر لإيقاف تنفيذه، يمكنك استخدام <kbd>Control</kbd> + <kbd>C</kbd> لقتل العملية. هذا سينهي الأمر ويُنشئ موجهًا جديدًا، مما يسمح لك بالاستمرار في العمل على ما تحتاج إليه. بالنسبة لمستخدمي PowerShell، يُستخدم <kbd>Control</kbd> + <kbd>C</kbd> أيضًا لنسخ النص - ولن يعمل لإنهاء عملية إلا عندما لا يكون هناك نص محدد للنسخ.

الاختصارات التالية متاحة فقط في الطرفيات المبنية على *nix، ولا يوجد لها بديل في PowerShell.

قد تحتاج أحيانًا إلى تعدد المهام، مما يسمح لعملية أو أمر بالعمل في الخلفية أثناء عملك على شيء آخر. الضغط على <kbd>Control</kbd> + <kbd>Z</kbd> يضع العملية الحالية في مهمة خلفية ويعيدك إلى سطر الأوامر لتواصل عملك. عندما تحتاج إلى إعادة التركيز على المهمة الخلفية، يمكنك استخدام `fg` لاستعادتها.

وأخيرًا، قد تحتاج إلى تشغيل أوامر مرتين متتاليتين. يمكنك الضغط على مفتاح <kbd>سهم للأعلى</kbd> للتنقل للخلف إلى الأمر الذي تم تشغيله سابقًا، أو يمكنك كتابة علامتي تعجب (`!!`) في سطر الأوامر والضغط على <kbd>Enter</kbd> - هذا سيشغل آخر أمر تم تنفيذه تلقائيًا.

<table>
  <caption>اختصارات ومُدخلات سطر الأوامر</caption>
  <thead>
    <tr>
      <th scope="col">الإجراء</th>
      <th scope="col">المُدخل</th>
      <th scope="col">التوفر أو السياق</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">التنقل للخلف عبر سجل الأوامر</th>
      <td><kbd>سهم للأعلى</kbd></td>
      <td>Linux، macOS، وPowerShell</td>
    </tr>
    <tr>
      <th scope="row">التنقل للأمام عبر سجل الأوامر</th>
      <td><kbd>سهم للأسفل</kbd></td>
      <td>Linux، macOS، وPowerShell</td>
    </tr>
    <tr>
      <th scope="row">إكمال اقتراح (الإكمال التلقائي باستخدام Tab)</th>
      <td><kbd>Tab</kbd></td>
      <td>Linux، macOS، وPowerShell (مصدر الاقتراح يختلف حسب الصدع - على سبيل المثال، يستخدم zsh سجل الأوامر الأخير، بينما يستخدم PowerShell الأوامر والمتغيرات والمعلمات المتاحة)</td>
    </tr>
    <tr>
      <th scope="row">مسح الطرفية</th>
      <td><kbd>Control</kbd> + <kbd>L</kbd></td>
      <td>Linux، macOS، وPowerShell (أمر <code>cls</code> في PowerShell يمسح الشاشة بنفس الطريقة)</td>
    </tr>
    <tr>
      <th scope="row">مقاطعة عملية جارية</th>
      <td><kbd>Control</kbd> + <kbd>C</kbd></td>
      <td>Linux، macOS، وPowerShell (في PowerShell يُستخدم أيضًا لنسخ النص، وينهي العملية فقط عندما لا يكون هناك نص محدد للنسخ)</td>
    </tr>
    <tr>
      <th scope="row">نقل عملية إلى الخلفية</th>
      <td><kbd>Control</kbd> + <kbd>Z</kbd></td>
      <td>Linux وmacOS فقط، لا يوجد بديل في PowerShell</td>
    </tr>
    <tr>
      <th scope="row">إعادة تشغيل الأمر السابق</th>
      <td><code>!!</code></td>
      <td>Linux وmacOS فقط، لا يوجد بديل في PowerShell</td>
    </tr>
  </tbody>
</table>

تقدم العديد من الطرفيات المزيد من الاختصارات، وأشجعك على قراءة الوثائق الخاصة بإعدادك لتجد الاختصارات التي تناسبك. لكن هذه الاختصارات الأساسية العالمية يجب أن تكون نقطة انطلاق رائعة لتعظيم إنتاجيتك وتصبح خبيرًا في الطرفية.

# --questions--

## --text--

ما هو تركيبة المفاتيح المستخدمة لمسح الشاشة في الطرفيات المبنية على *nix؟

## --answers--

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

فكر في أي اختصار ينشئ موجهًا نظيفًا وجديدًا.

---

<kbd>Control</kbd> + <kbd>L</kbd>

---

<kbd>Control</kbd> + <kbd>Z</kbd>

### --feedback--

فكر في أي اختصار ينشئ موجهًا نظيفًا وجديدًا.

---

مفتاح <kbd>Tab</kbd>

### --feedback--

فكر في أي اختصار ينشئ موجهًا نظيفًا وجديدًا.

## --video-solution--

2

## --text--

ماذا يحدث عند الضغط على <kbd>Control</kbd> + <kbd>Z</kbd> في طرفية مبنية على *nix؟

## --answers--

ينهي العملية الحالية.

### --feedback--

هذا الاختصار يسمح لك بتعدد المهام أثناء تشغيل عملية.

---

يضع العملية الحالية في الخلفية.

---

يمسح الشاشة.

### --feedback--

هذا الاختصار يسمح لك بتعدد المهام أثناء تشغيل عملية.

---

يتنقل عبر سجل الأوامر.

### --feedback--

هذا الاختصار يسمح لك بتعدد المهام أثناء تشغيل عملية.

## --video-solution--

2

## --text--

أي مما يلي يمكن استخدامه لتشغيل آخر أمر تم تنفيذه بسرعة في طرفية مبنية على *nix؟

## --answers--

<kbd>Control</kbd> + <kbd>L</kbd>

### --feedback--

هذا يتطلب كتابة حرف معين مرتين.

---

مفتاح <kbd>Tab</kbd>

### --feedback--

هذا يتطلب كتابة حرف معين مرتين.

---

`!!`

---

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

هذا يتطلب كتابة حرف معين مرتين.

## --video-solution--

3
