---
id: 672d45651d83b450801efb3a
title: 如何在 VS Code 中创建项目并在本地运行代码
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

首先，你应该知道 VS Code 将项目视为“工作区”。 工作区就是你在 VS Code 中打开的目录。

首先，你需要使用命令行界面为你的新项目创建一个新的目录。目录是文件夹的另一种叫法。

命令行界面，简称 CLI，允许你通过基于文本的命令与操作系统交互。你将在后续课程中学习更多关于这些工具的内容。

如果你使用的是 Windows 系统，可以使用命令提示符或 PowerShell。 如果你使用的是 Mac，可以打开终端应用程序。

命令行打开后，导航到你的主目录。在命令提示符中，输入 `cd /d %USERPROFILE%` 并按键盘上的 `Enter`。在 PowerShell 或 macOS 终端中，输入 `cd ~` 并按 `Enter`。然后输入 `mkdir my-project` 命令并按 `Enter`。`mkdir`，即“创建目录”，是用来创建新目录的命令。接着你应该用 VS Code 打开你新创建的目录。

在 VS Code 中还有其他打开目录的方法。

根据操作系统的不同，你可能有一个上下文菜单（或右键单击选项），可以直接从文件资源管理器打开 VS Code。 你也可以选择双击 VS Code 应用程序。 如果你对命令行操作熟练，可以运行 `code /path/to/folder` 命令。

打开后，你会看到一个新的空白工作区。

要创建新文件，点击左上角的 `File` 菜单并选择 `New File...`。然后将文件命名为 `index.html` 并点击 `Save`。

现在，你应该能在资源管理器选项卡中看到新文件了。 尝试在 `index.html` 文件中添加一些 HTML。

现在你可能很想直接在浏览器中打开 HTML 文件。 虽然这种方法可以奏效，但也可能导致各种意想不到的错误，例如 CSS 无法正常加载。

相反，你应该将 HTML 文件放在一个适当的网络服务器后面。 如果你是编码新手，这听起来可能是一项艰巨的任务，但实际上有一种方法可以从 VS Code 中直接完成这项任务。

你需要先获取 Live Server 扩展（程序、功能），它可以在市场中免费获得。

点击左侧边栏的扩展（程序、功能）标签（页），然后在搜索栏中输入 `Live Server`。

安装完成后，你应该会在状态栏看到一个 `Go Live` 按钮。确保你已经打开了 HTML 文件，然后点击它。

你的新页面应该会自动在浏览器的新标签（页）中打开。如果没有，你可以直接访问 `http://localhost:5500/`。

恭喜！你现在已经在 VS Code 中设置了你自己的项目，并可以通过 Live Server 查看你的更改。

# --questions--

## --text--

Visual Studio Code 讲什么视为“工作区”？

## --answers--

整个硬盘。

### --feedback--

本课提到 VS Code 如何定义与目录相关的工作区。

---

你在 VS Code 中打开的目录。

---

在 VS Code 中打开所有文件。

### --feedback--

本课提到 VS Code 如何定义与目录相关的工作区。

---

VS Code 应用程序文件夹。

### --feedback--

本课提到 VS Code 如何定义与目录相关的工作区。

## --video-solution--

2

## --text--

为什么课程建议使用网络服务器而不是直接在浏览器中打开超文本标记语言文件？

## --answers--

加载速度更快，更容易发现代码库中的问题。

### --feedback--

课程提到直接打开超文本标记语言 文件时可能出现的特定问题。

---

它为大型软件应用程序提供了更好的安全性。

### --feedback--

课程提到直接打开超文本标记语言 文件时可能出现的特定问题。

---

它可以避免意想不到的错误，例如 CSS 无法正常加载。

---

它允许与团队中的其他开发人员进行实时协作。

### --feedback--

课程提到直接打开超文本标记语言 文件时可能出现的特定问题。

## --video-solution--

3

## --text--

建议使用哪种 VS Code 扩展来轻松设置本地网络服务器？

## --answers--

网络服务器。

### --feedback--

课程特别提到从市场安装一个扩展（程序、功能）来服务你的 HTML 文件。

---

实时服务器。

---

本地环境。

### --feedback--

课程特别提到从市场安装一个扩展（程序、功能）来服务你的 HTML 文件。

---

HTML 预览。

### --feedback--

课程特别提到从市场安装一个扩展（程序、功能）来服务你的 HTML 文件。

## --video-solution--

2
