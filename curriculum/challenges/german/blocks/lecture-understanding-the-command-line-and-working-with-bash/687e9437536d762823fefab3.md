---
id: 687e9437536d762823fefab3
title: "Welche Tastenkombinationen können Sie in der Befehlszeile verwenden, um die Produktivität zu steigern?"
challengeType: 19
dashedName: what-are-some-shortcuts-you-can-use-in-the-command-line-to-speed-up-productivity
---

# --description--

Lernen wir einige Tastenkombinationen in der Befehlszeile kennen, mit denen Sie Ihre Produktivität steigern können.

Terminal- und Befehlszeilen-Tastenkombinationen können Ihnen helfen, Ihre Arbeitsabläufe zu optimieren und Ihre Produktivität zu maximieren. Für Linux und macOS, die beide ihre Wurzeln in Unix haben, sind viele dieser Tastenkombinationen gleich. Für Windows gibt es jedoch einige Unterschiede.

Die erste Tastenkombination ist die <kbd>Pfeil nach oben</kbd>-Taste. Wenn Sie diese Taste drücken, können Sie rückwärts durch Ihre Befehls-Historie blättern, jeweils einen Befehl nach dem anderen. Die <kbd>Pfeil nach unten</kbd>-Taste können Sie dann verwenden, um vorwärts durch Ihre Befehls-Historie zu blättern. Diese beiden Tasten ermöglichen es Ihnen, schnell durch die zuvor ausgeführten Befehle zu navigieren, um einen Befehl zu finden, den Sie erneut ausführen möchten.

Viele Shells bieten auch eine Auto-Vervollständigungsfunktion an, die Befehle vorschlägt, basierend auf dem, was Sie zu tippen begonnen haben. Die <kbd>Tab</kbd>-Taste kann verwendet werden, um den Rest des Vorschlags auszufüllen und so Ihre Kommandozeile schnell mit der vollständigen Syntax zu befüllen. Wie diese Vorschläge generiert werden, variiert von Shell zu Shell. Zum Beispiel basieren diese Vorschläge in zsh auf Ihrer jüngsten Befehls-Historie. In PowerShell hingegen basieren sie auf verfügbaren Befehlen, Variablen und Argumenten (wobei weniger Gewicht darauf liegt, wie kürzlich sie verwendet wurden).

Manchmal kann ein Befehl eine große Menge an Ausgabe in Ihrem Terminal erzeugen. Dieses zusätzliche Rauschen kann stören oder Ihren Arbeitsablauf unterbrechen. In *nix-basierten Terminals, die auf Linux- und macOS-Geräten zu finden sind, können Sie <kbd>Control</kbd> + <kbd>L</kbd> drücken, um den gesamten Bildschirm zu löschen und mit einer frischen, sauberen Eingabeaufforderung zu beginnen. In PowerShell löscht <kbd>Control</kbd> + <kbd>L</kbd> standardmäßig ebenfalls den Bildschirm, und Sie können den Befehl `cls` ausführen, um dasselbe Ergebnis zu erzielen.

Wenn Sie einen Befehl unterbrechen müssen, um seine Ausführung zu stoppen, können Sie <kbd>Control</kbd> + <kbd>C</kbd> verwenden, um den Prozess zu beenden. Dies beendet den Befehl und erzeugt eine neue Eingabeaufforderung, sodass Sie mit allem anderen fortfahren können, woran Sie arbeiten müssen. Für PowerShell-Nutzer wird <kbd>Control</kbd> + <kbd>C</kbd> auch zum Kopieren von Text verwendet – und beendet einen Prozess nur dann, wenn der Kontext eindeutig ist (zum Beispiel wenn kein Text zum Kopieren ausgewählt ist).

Die nächsten Tastenkombinationen sind nur in *nix-basierten Terminals verfügbar und haben keine PowerShell-Alternative.

Es kann vorkommen, dass Sie multitasken müssen, also einen Prozess oder Befehl im Hintergrund laufen lassen, während Sie an einem anderen arbeiten. Wenn Sie <kbd>Control</kbd> + <kbd>Z</kbd> drücken, wird der aktuelle Prozess in eine Hintergrund-Aufgabe verschoben und Sie kehren zur Kommandozeile zurück, wo Sie Ihre Arbeit fortsetzen können. Wenn Sie den Fokus wieder auf die Hintergrund-Aufgabe legen müssen, können Sie `fg` verwenden, um sie wiederherzustellen.

Und schließlich gibt es Befehle, die Sie zweimal hintereinander ausführen müssen. Sie können die <kbd>Pfeil nach oben</kbd>-Taste drücken, um rückwärts zum zuletzt ausgeführten Befehl zu blättern, oder Sie geben zwei Ausrufezeichen (`!!`) in die Kommandozeile ein und drücken <kbd>Enter</kbd> – damit wird automatisch der zuletzt ausgeführte Befehl erneut ausgeführt.

<table>
  <caption>Befehlszeilenkürzel und Eingaben</caption>
  <thead>
    <tr>
      <th scope="col">Aktion</th>
      <th scope="col">Eingabe</th>
      <th scope="col">Verfügbarkeit oder Kontext</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">Rückwärts durch die Befehls-Historie bewegen</th>
      <td><kbd>Pfeil nach oben</kbd></td>
      <td>Linux, macOS und PowerShell</td>
    </tr>
    <tr>
      <th scope="row">Vorwärts durch die Befehls-Historie bewegen</th>
      <td><kbd>Pfeil nach unten</kbd></td>
      <td>Linux, macOS und PowerShell</td>
    </tr>
    <tr>
      <th scope="row">Vervollständigen eines Vorschlags (Tab-Vervollständigung)</th>
      <td><kbd>Tab</kbd></td>
      <td>Linux, macOS und PowerShell (die Quelle der Vorschläge variiert je nach Shell – z. B. verwendet zsh die jüngste Befehls-Historie, während PowerShell verfügbare Befehle, Variablen und Argumente nutzt)</td>
    </tr>
    <tr>
      <th scope="row">Terminal löschen</th>
      <td><kbd>Control</kbd> + <kbd>L</kbd></td>
      <td>Linux, macOS und PowerShell (PowerShells <code>cls</code>-Befehl löscht den Bildschirm auf dieselbe Weise)</td>
    </tr>
    <tr>
      <th scope="row">Einen laufenden Prozess unterbrechen</th>
      <td><kbd>Control</kbd> + <kbd>C</kbd></td>
      <td>Linux, macOS und PowerShell (in PowerShell kopiert dies auch Text und beendet einen Prozess nur, wenn nichts zum Kopieren ausgewählt ist)</td>
    </tr>
    <tr>
      <th scope="row">Einen Prozess in den Hintergrund verschieben</th>
      <td><kbd>Control</kbd> + <kbd>Z</kbd></td>
      <td>Nur Linux und macOS, keine PowerShell-Alternative</td>
    </tr>
    <tr>
      <th scope="row">Den vorherigen Befehl erneut ausführen</th>
      <td><code>!!</code></td>
      <td>Nur Linux und macOS, keine PowerShell-Alternative</td>
    </tr>
  </tbody>
</table>

Viele Terminals bieten noch mehr Tastenkombinationen, und ich empfehle Ihnen, die Dokumentation für Ihre spezielle Umgebung zu lesen, um die für Sie besten Tastenkombinationen zu finden. Diese grundlegenden universellen Tastenkombinationen sind jedoch ein großartiger Ausgangspunkt, um Ihre Produktivität zu maximieren und ein Terminal-Profi zu werden.

# --questions--

## --text--

Welche Tastenkombination wird verwendet, um den Bildschirm in *nix-basierten Terminals zu löschen?

## --answers--

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

Überlegen Sie, welche Tastenkombination eine frische, saubere Eingabeaufforderung erzeugt.

---

<kbd>Control</kbd> + <kbd>L</kbd>

---

<kbd>Control</kbd> + <kbd>Z</kbd>

### --feedback--

Überlegen Sie, welche Tastenkombination eine frische, saubere Eingabeaufforderung erzeugt.

---

Die <kbd>Tab</kbd>-Taste

### --feedback--

Überlegen Sie, welche Tastenkombination eine frische, saubere Eingabeaufforderung erzeugt.

## --video-solution--

2

## --text--

Was passiert, wenn Sie in einem *nix-basierten Terminal <kbd>Control</kbd> + <kbd>Z</kbd> drücken?

## --answers--

Der aktuelle Prozess wird beendet.

### --feedback--

Diese Tastenkombination ermöglicht Multitasking, während ein Prozess läuft.

---

Der aktuelle Prozess wird in den Hintergrund verschoben.

---

Der Bildschirm wird gelöscht.

### --feedback--

Diese Tastenkombination ermöglicht Multitasking, während ein Prozess läuft.

---

Es wird durch die Befehls-Historie geblättert.

### --feedback--

Diese Tastenkombination ermöglicht Multitasking, während ein Prozess läuft.

## --video-solution--

2

## --text--

Welche der folgenden Möglichkeiten können verwendet werden, um den zuletzt ausgeführten Befehl in einem *nix-basierten Terminal schnell auszuführen?

## --answers--

<kbd>Control</kbd> + <kbd>L</kbd>

### --feedback--

Dabei wird ein bestimmtes Zeichen zweimal eingegeben.

---

Die <kbd>Tab</kbd>-Taste

### --feedback--

Dabei wird ein bestimmtes Zeichen zweimal eingegeben.

---

`!!`

---

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

Dabei wird ein bestimmtes Zeichen zweimal eingegeben.

## --video-solution--

3
