---
id: d634ed1a5f5e63b409d41ff5
title: Rückblick auf Rekursion
challengeType: 31
dashedName: review-recursion-python
---

# --description--

- Rekursion ist ein Programmierkonzept, das es Ihnen ermöglicht, eine Funktion wiederholt aufzurufen, bis ein Basisfall erreicht ist.

Hier ist ein Beispiel für eine rekursive Funktion, die die Fakultät einer Zahl berechnet:

```py
def find_factorial(n):
    if n == 0:
        return 1
    return n * find_factorial(n - 1)
```

Im obigen Beispiel wird die `find_factorial`-Funktion rekursiv aufgerufen, bis `n` den Wert `0` erreicht. Wenn `n` `0` ist, wird der Basisfall erreicht und die Funktion gibt `1` zurück. Die Funktion gibt dann das Produkt von `n` und dem Ergebnis des rekursiven Aufrufs von `find_factorial(n - 1)` zurück.

- Rekursion ermöglicht es Ihnen, etwas mit unbekannter Tiefe zu verarbeiten, wie zum Beispiel tief verschachtelte Dictionaries und Listen oder einen Dateibaum.
- Ein Aufrufstapel wird verwendet, um die Funktionsaufrufe in einer rekursiven Funktion zu verfolgen. Jedes Mal, wenn eine Funktion aufgerufen wird, wird sie dem Aufrufstapel hinzugefügt. Wenn der Basisfall erreicht ist, werden die Funktionsaufrufe aus dem Stapel entfernt.
- Sie sollten den Basisfall sorgfältig definieren. Andernfalls kann die Funktion sich selbst immer wieder aufrufen, bis Python einen `RecursionError` auslöst.
- Rekursion ist nützlich zur Lösung mathematischer Probleme wie Fakultät und Fibonacci, zum Durchlaufen von Bäumen und Graphen sowie zum Erzeugen von Permutationen und Kombinationen.

# --assignment--

Überprüfen Sie die Themen und Konzepte zur Rekursion.
