---
id: f0b22197f53b2e630a538090
title: 再帰のクイズ
challengeType: 8
dashedName: quiz-recursion-python
---

# --description--

クイズに合格するには、以下の10問中少なくとも9問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

プログラミングにおける再帰とは何ですか？

#### --distractors--

リストをソートする方法です。

---

終わらないループ。

---

`None` を返す関数です。

#### --answer--

関数が自分自身を呼び出すプロセス。

### --question--

#### --text--

次のうち再帰の例はどれですか？

#### --distractors--

```py
def factorial(n):
    result = 1
    while n > 0:
        result *= n
        n -= 1
    return result
```

---

```py
def factorial(n):
    numbers = [number for number in range(1, n + 1)]
    result = 1
    for number in numbers:
        result *= number
    return result
```

---

```py
def factorial(n):
    result = 1
    for number in range(n, 0, -1):
        result *= number
    return result
```

#### --answer--

```py
def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)
```

### --question--

#### --text--

次の関数は何を返しますか？

```py
def sum_numbers(n):
    if n == 0:
        return 0
    return n + sum_numbers(n - 1)

sum_numbers(3)
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

`mystery`関数は何回呼び出されますか？

```py
def mystery(n):
    if n <= 1:
        return 1
    return mystery(n - 2)

mystery(5)
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

再帰について正しいのはどれですか？

#### --distractors--

基本ケースを持ってはいけない。

---

フィボナッチ数列にのみ使うべき。

---

階乗にのみ使うべき。

#### --answer--

必ず基本ケースを持つべき。

### --question--

#### --text--

与えられた例に適切な基本ケースはどれですか？

```py
def count_down_to_zero(number):
    # Base case goes here.

    print(number)
    count_down_to_zero(number - 1)
```

#### --distractors--

```py
if number > 0:
    return
```

---

```py
if number != 0:
    return
```

---

```py
if number == 0:
    return
```

#### --answer--

```py
if number < 0:
    return
```

### --question--

#### --text--

なぜ再帰には基本ケースが必要ですか？

#### --distractors--

関数が常に戻り値を持つようにするため。

---

関数呼び出しの回数を減らすため。

---

すべての再帰呼び出しが正しい順序で実行されることを保証するためです。

#### --answer--

関数が再帰呼び出しを停止し、`RecursionError` を回避する方法を提供するためです。

### --question--

#### --text--

この再帰関数は何をしますか？

```py
def repeat_string(string):
    return string + repeat_string(string)
```

#### --distractors--

文字列を2回返す。

---

`None` を返します。

---

空の文字列を作る。

#### --answer--

`RecursionError` を発生させます。

### --question--

#### --text--

コールスタックとは何ですか？

#### --distractors--

実行された関数呼び出しのリスト。

---

関数が返した値のリスト。

---

関数をちょうど3回呼び出すための特別な関数。

#### --answer--

関数呼び出しとその実行順序を管理するデータ構造。

### --question--

#### --text--

この再帰関数は何をしますか？

```py
def change_string(string):
    if string == "":
        return ""
    return change_string(string[1:]) + string[0]
```

#### --distractors--

文字列を複製する。

---

母音を取り除く。

---

スペースを取り除く。

#### --answer--

文字列を逆順にする。
