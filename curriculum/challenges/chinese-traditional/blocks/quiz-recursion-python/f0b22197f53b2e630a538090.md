---
id: f0b22197f53b2e630a538090
title: 遞迴小測驗
challengeType: 8
dashedName: quiz-recursion-python
---

# --description--

要通過這個小測驗，你必須在以下 10 題中正確回答至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是程式設計中的遞迴？

#### --distractors--

一種排序列表的方法。

---

一個永不結束的迴圈。

---

一個傳回 `None` 的函式（程式）。

#### --answer--

一個函式呼叫自身的處理程序。

### --question--

#### --text--

以下哪一項是遞迴的範例？

#### --distractors--

```py
def factorial(n):
    result = 1
    while n > 0:
        result *= n
        n -= 1
    return result
```

---

```py
def factorial(n):
    numbers = [number for number in range(1, n + 1)]
    result = 1
    for number in numbers:
        result *= number
    return result
```

---

```py
def factorial(n):
    result = 1
    for number in range(n, 0, -1):
        result *= number
    return result
```

#### --answer--

```py
def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)
```

### --question--

#### --text--

以下函式會傳回什麼？

```py
def sum_numbers(n):
    if n == 0:
        return 0
    return n + sum_numbers(n - 1)

sum_numbers(3)
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

`mystery` 函式（程式）會被呼叫多少次？

```py
def mystery(n):
    if n <= 1:
        return 1
    return mystery(n - 2)

mystery(5)
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

以下哪一項關於遞迴是正確的？

#### --distractors--

它絕不應該有基底案例。

---

它應該只用於 Fibonacci 數列。

---

它應該只用於階乘。

#### --answer--

它應該始終有一個基本情況。

### --question--

#### --text--

以下哪一個選項，可作為給定範例的適當基底案例？

```py
def count_down_to_zero(number):
    # Base case goes here.

    print(number)
    count_down_to_zero(number - 1)
```

#### --distractors--

```py
if number > 0:
    return
```

---

```py
if number != 0:
    return
```

---

```py
if number == 0:
    return
```

#### --answer--

```py
if number < 0:
    return
```

### --question--

#### --text--

為什麼遞迴必須有基底情況？

#### --distractors--

為了確保函式（程式）總是有返回值。

---

為了減少函式（程式）呼叫的次數。

---

確保所有遞迴呼叫都依正確順序執行。

#### --answer--

提供函式停止遞迴呼叫並避免 `RecursionError` 的方式。

### --question--

#### --text--

這個遞迴函式（程式）會做什麼？

```py
def repeat_string(string):
    return string + repeat_string(string)
```

#### --distractors--

傳回該字串兩次。

---

傳回 `None`。

---

創建一個空字串。

#### --answer--

引發 `RecursionError`。

### --question--

#### --text--

什麼是呼叫堆疊？

#### --distractors--

已執行的函式呼叫列表。

---

由函式傳回的值的列表。

---

一個用來精確呼叫函式三次的特殊函式。

#### --answer--

一個追蹤函式（程式）呼叫及其執行順序的資料結構。

### --question--

#### --text--

這個遞迴函式（程式）做什麼？

```py
def change_string(string):
    if string == "":
        return ""
    return change_string(string[1:]) + string[0]
```

#### --distractors--

複製字串。

---

移除母音。

---

移除空格。

#### --answer--

反轉字串。
