---
id: 67f39b40deaec81a3e40e0c5
title: مراجعة أساسيات Python
challengeType: 31
dashedName: review-python-basics
---

# --description--

## ما هي بايثون؟

- **مقدمة**: بايثون هي لغة برمجة عامة الغرض معروفة ببساطتها وسهولة استخدامها. تُستخدم بايثون في مجالات عديدة مثل علم البيانات وتعلم الآلة، تطوير الويب، البرمجة النصية والأتمتة، الأنظمة المدمجة وإنترنت الأشياء، وأكثر من ذلك.
- **حالات الاستخدام الشائعة**: تُستخدم بايثون في علم البيانات، تعلم الآلة، تطوير الويب، الأمن السيبراني، الأتمتة، وأجهزة الحاسوب الصغيرة مثل Raspberry Pi واللوحات المتوافقة مع MicroPython.

## المتغيرات

- **إعلان المتغيرات**: لإعلان متغير، تبدأ باسم المتغير متبوعًا بمعامل التعيين (`=`) ثم القيمة. يمكن أن تكون القيمة رقمًا، سلسلة نصية، قيمة منطقية، إلخ. إليك بعض الأمثلة:

```py
name = 'John Doe'
age = 25
```

- **اصطلاحات تسمية المتغيرات**: إليك الاصطلاحات التي يجب استخدامها لتسمية المتغيرات:

  - أسماء المتغيرات يمكن أن تبدأ فقط بحرف أو بشرطة سفلية (_) وليس برقم.
  - أسماء المتغيرات يمكن أن تحتوي فقط على أحرف أبجدية رقمية (a-z, A-Z, 0-9) والشرطة السفلية (_).
  - أسماء المتغيرات حساسة لحالة الأحرف — `age`، `Age`، و`AGE` تعتبر أسماء مختلفة.
  - لا يمكن أن تكون أسماء المتغيرات من الكلمات المفتاحية المحجوزة في Python مثل `if`، `class`، أو `def`.
  - أسماء المتغيرات متعددة الكلمات تُفصل بالشرطة السفلية. مثال: `snake_case`.


## التعليقات

- **تعليقات السطر الواحد**: يجب استخدام هذه الأنواع من التعليقات للملاحظات القصيرة التي ترغب في تركها داخل الكود.

```py
# This is a single line comment
```

- **تعليقات متعددة الأسطر**: يمكنك استخدام `#` في بداية كل سطر لإنشاء تعليقات تمتد عبر عدة أسطر.

```py
# This is a multi-line comment.
# Here is some code commented out.
#
# name = 'John Doe'
# age = 25
```

- **دالة `print()`**: لطباعة البيانات على الطرفية، يمكنك استخدام الدالة `print()` بهذه الطريقة:

```py
print('Hello world!') # Hello world!
```

## أنواع البيانات الشائعة في بايثون

- **مقدمة**: Python لغة ذات نوع ديناميكي، مما يعني أنك لا تحتاج إلى إعلان نوع المتغير. Python تحدد النوع بناءً على القيمة المُعيَّنة للمتغير.
- **عدد صحيح**: رقم كامل بدون كسور عشرية:

```py
my_integer_var = 10
print('Integer:', my_integer_var) # Integer: 10
```

- **عدد عشري (float)**: رقم يحتوي على كسور عشرية:

```py
my_float_var = 4.50
print('Float:', my_float_var) # Float: 4.5
```

- **سلسلة نصية**: متتالية من الأحرف محاطة بعلامات اقتباس:

```py
my_string_var = 'hello'
print('String:', my_string_var) # String: hello
```

- **قيمة منطقية**: قيمة تمثل إما `True` أو `False`:

```py
my_boolean_var = True
print('Boolean:', my_boolean_var) # Boolean: True
```

لدى Python أنواع بيانات أخرى. ستتعلم كل نوع منها عند حاجتك إليه لأول مرة.

- **دالة `type()`**: لرؤية نوع المتغير، يمكنك استخدام الدالة `type()` بهذه الطريقة:

```py
greeting = 'Hello there!'
age = 21

print(type(greeting)) # <class 'str'>
print(type(age)) # <class 'int'>
```

- **دالة `isinstance()`**: تُستخدم للتحقق مما إذا كان المتغير يطابق نوع بيانات معين:

```py
greeting = 'Hello world'
name = 'John Doe'

print(isinstance(greeting, str)) # True
print(isinstance(name, int)) # False
```

## العمل مع السلاسل النصية

- **تعريف**: السلاسل النصية غير قابلة للتغيير، مما يعني أنك لا تستطيع تعديلها بعد إنشائها. في بايثون، يمكنك استخدام علامات اقتباس مفردة أو علامات اقتباس مزدوجة. اختر نمطًا واحدًا واستخدمه باستمرار:

```py
developer = 'Jessica'
city = "Los Angeles"
```

- **الوصول إلى الأحرف في السلاسل النصية**: يمكنك الوصول إلى الأحرف باستخدام ترميز الأقواس هكذا:

```py
my_str = 'Hello world'

print(my_str[0])  # H
print(my_str[6])  # w

print(my_str[-1])  # d
print(my_str[-2]) # l
```

- **الهروب في السلاسل النصية**: يمكنك استخدام الشرطة المائلة العكسية (`\`) إذا كانت السلسلة تحتوي على علامات اقتباس هكذا:

```py
msg = 'It\'s a sunny day'
quote = "She said, \"Hello!\""
```

- **ربط السلاسل النصية**: لربط السلاسل النصية، يمكنك استخدام معامل `+` هكذا:

```py
developer = 'Jessica'
print('My name is ' + developer + '.') # My name is Jessica.
```

طريقة أخرى لربط السلاسل النصية هي باستخدام معامل `+=`. يُستخدم هذا لأداء الربط والتعيين في خطوة واحدة هكذا:

```py
greeting = 'My name is '
developer = 'Jessica.'

greeting += developer
print(greeting) # My name is Jessica.
```

- **`f-strings`**: اختصار لـ formatted string literals. تتيح لك التعامل مع الاستيفاء وأيضًا إجراء بعض الربط بصيغة مختصرة وقابلة للقراءة:

```py
developer = 'Jessica'
greeting = f'My name is {developer}.'
print(greeting) # My name is Jessica.
```

- **تقطيع السلاسل النصية**: هو استخراج أجزاء من السلسلة النصية. الصيغة الأساسية هي:

```py
str[start:stop:step]
```

تمثل نقطة البداية الفهرس الذي يبدأ منه الاستخراج. نقطة النهاية هي حيث ينتهي التقطيع، وهذه النقطة غير شاملة. تمثل خطوة التقطيع الفاصل الزمني للزيادة. إليك بعض الأمثلة:

```py
message = 'Python is fun!'

print(message[0:6])  # Python
print(message[7:])  # is fun!
print(message[::2])  # Pto sfn
```

- **الحصول على طول السلسلة النصية**: تُستخدم الدالة `len()` لإرجاع عدد الأحرف في السلسلة:

```py
developer = 'Jessica'

print(len(developer)) # 7
```

## العمل مع معامل `in`

- **معامل `in`**: يُرجع قيمة منطقية تحدد ما إذا كان الحرف أو الأحرف موجودة في السلسلة النصية أم لا:

```py
my_str = 'Hello world'

print('Hello' in my_str)  # True
print('hey' in my_str)    # False
print('hi' in my_str)    # False
print('e' in my_str)  # True
print('f' in my_str)  # False
```

## طرق السلاسل النصية الشائعة

- **`str.upper()`**: تُرجع سلسلة جديدة مع تحويل جميع الأحرف إلى أحرف كبيرة:

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

- **`str.lower()`**: تُرجع سلسلة جديدة مع تحويل جميع الأحرف إلى أحرف صغيرة:

```py
developer = 'Jessica'

print(developer.lower()) # jessica
```

- **`str.strip()`**: تُرجع نسخة من السلسلة مع إزالة الأحرف المحددة من البداية والنهاية (إذا لم تُمرر معلمة، تزيل الفراغات من البداية والنهاية).

```py
greeting = '  hello world  '

trimmed_my_str = greeting.strip()
print(trimmed_my_str)  # 'hello world'
```

- **`replace()`**: تُرجع سلسلة جديدة مع استبدال كل تكرارات السلسلة القديمة بسلسلة جديدة.

```py
greeting = 'hello world'

replaced_my_str = greeting.replace('hello', 'hi')
print(replaced_my_str)  # 'hi world'
```

- **`split()`**: تُستخدم لتقسيم سلسلة نصية إلى قائمة باستخدام فاصل محدد. الفاصل هو سلسلة تحدد مكان التقسيم.

```py
dashed_name = 'example-dashed-name'

split_words = dashed_name.split('-')
print(split_words)  # ['example', 'dashed', 'name']
```

- **`join()`**: تُستخدم لدمج مجموعة من السلاسل النصية في سلسلة واحدة مع فاصل محدد.

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # example dashed name
```

- **`str.startswith(prefix)`**: تُرجع قيمة منطقية تشير إلى ما إذا كانت السلسلة تبدأ بالبادئة المحددة:

```py
developer = 'Naomi'

result = developer.startswith('N')
print(result)  # True
```

- **`str.endswith(suffix)`**: تُرجع قيمة منطقية تشير إلى ما إذا كانت السلسلة تنتهي باللاحقة المحددة:

```py
developer = 'Naomi'

result = developer.endswith('N')
print(result)  # False
```

- **`str.find()`**: تُرجع الفهرس لأول ظهور لسلسلة فرعية. إذا لم تُعثر عليها، تُرجع `-1`:

```py
developer = 'Naomi'

result = developer.find('N')
print(result)  # 0

city = 'Los Angeles'
print(city.find('New')) # -1
```

- **`str.count(substring)`**: تحصي عدد مرات ظهور سلسلة فرعية في السلسلة:

```py
city = 'Los Angeles'
print(city.count('e')) # 2
```

- **`str.capitalize()`**: تُرجع سلسلة جديدة مع تحويل الحرف الأول إلى كبير وباقي الأحرف إلى صغيرة:

```py
dessert = 'chocolate cake'
print(dessert.capitalize()) # Chocolate cake
```

- **`str.isupper()`**: تُرجع `True` إذا كانت كل الأحرف في السلسلة كبيرة و`False` خلاف ذلك:

```py
dessert = 'chocolate cake'
print(dessert.isupper()) # False
```

- **`str.islower()`**: تُرجع `True` إذا كانت كل الأحرف في السلسلة صغيرة و`False` خلاف ذلك:

```py
dessert = 'chocolate cake'
print(dessert.islower()) # True
```

- **`str.title()`**: تُرجع سلسلة جديدة مع تحويل الحرف الأول من كل كلمة إلى كبير:

```py
city = 'los angeles'
print(city.title()) # Los Angeles
```

- **`str.maketrans()`**: تُستخدم هذه الطريقة لإنشاء جدول تحويل 1 إلى 1 للأحرف للترجمة. غالبًا ما تُستخدم مع طريقة `translate()` التي تطبق هذا الجدول على السلسلة وتُرجع النتيجة المترجمة.

```py
trans_table = str.maketrans('abc', '123')
print(trans_table) # {97: 49, 98: 50, 99: 51}

result = 'abcabc'.translate(trans_table)
print(result)  # 123123
```

## العمليات الشائعة مع الأعداد الصحيحة والعشرية

- **العمليات الحسابية الأساسية**: في بايثون، يمكنك إجراء عمليات حسابية أساسية مع الأعداد الصحيحة والعشرية مثل الجمع، الطرح، الضرب، والقسمة:

```py
int_1 = 56
int_2 = 12
float_1 = 5.4
float_2 = 12.0

# Addition

print('Integer Addition:', int_1 + int_2) # Integer Addition: 68
print('Float Addition:', float_1 + float_2) # Float Addition: 17.4

# Subtraction

print('Int Subtraction:', int_1 - int_2) # Int Subtraction: 44
print('Float Subtraction:',  float_2 - float_1) # Float Subtraction: 6.6

# Multiplication

print('Int Multiplication:', int_1 * int_2) # Int Multiplication: 672
print('Float Multiplication:', float_2 * float_1) # Float Multiplication: 64.80000000000001

# Division

print('Division:', int_1 / int_2) # Division: 4.666666666666667
print('Float Division:', float_2 / float_1) # Float Division: 2.222222222222222
```

عند جمع عدد عشري وعدد صحيح، تتحول النتيجة إلى عدد عشري هكذا:

```py
int_1 = 56
float_1 = 5.4

print(int_1 + float_1) # 61.4
```

- **معامل باقي القسمة (`%`)**: يُرجع الباقي عند قسمة رقم على رقم آخر:

```py
int_1 = 56
int_2 = 12

print(int_1 % int_2) # 8
```

- **القسمة الأرضية (`//`)**: يُستخدم لقسمة رقمين وتقريب النتيجة إلى أقرب عدد صحيح أقل أو يساوي الناتج:

```py
int_1 = 56
int_2 = 12

print(int_1 // int_2) # 4
```

- **معامل الأس (`**`)**: يُستخدم لرفع رقم إلى قوة رقم آخر:

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # 16
```

- **الدالة `float()`**: يمكنك استخدام هذه الدالة لتحويل عدد صحيح إلى عدد عشري.

```py
num = 4

print(float(num)) # 4.0
```

- **الدالة `int()`**: يمكنك استخدام هذه الدالة لتحويل عدد عشري إلى عدد صحيح.

```py
num = 4.0

print(int(num)) # 4
```

- **دالة `round()`**: تُستخدم لتقريب رقم إلى أقرب عدد صحيح:

```py
num_1 = 3.4
num_2 = 7.7

print(round(num_1)) # 3
print(round(num_2)) # 8
```

- **دالة `abs()`**: تُستخدم لإرجاع القيمة المطلقة لرقم:

```py
num = -13

print(abs(num)) # 13
```

- **دالة `pow()`**: تُستخدم لرفع رقم إلى قوة رقم آخر:

```py
result = pow(2, 3) 
print(result)  # 8
```

## التعيينات المعززة

- **تعريف**: التعيين المعزز يطبق عملية على متغير ويخزن النتيجة في نفس المتغير، كل ذلك في خطوة واحدة.

```py
# Addition assignment 
my_var = 10
my_var += 5

print(my_var) # 15

# Subtraction assignment
count = 14
count -= 3

print(count) # 11

# Multiplication assignment 
product = 65
product *= 7

print(product) # 455

# Division assignment 
price = 100
price /= 4

print(price) # 25.0

# Floor Division assignment 
total_pages = 23
total_pages //= 5

print(total_pages) # 4

# Modulo assignment 
bits = 35
bits %= 2

print(bits) # 1

# Exponentiation assignment 
power = 2
power **= 3

print(power) # 8
```

## العمل مع الدوال

- **التعريف**: الدوال هي قطع قابلة لإعادة الاستخدام من الكود تأخذ مُدخلات (معلمات) وتُرجع مخرجًا. لاستدعاء دالة، يجب الإشارة إلى اسم الدالة متبوعًا بمجموعة من الأقواس:

```py
# Defining a function

def get_sum(num_1, num_2):
    return num_1 + num_2

result = get_sum(3, 4) # function call
print(result) # 7
```

إذا لم تُرجع الدالة قيمة صراحة، فإن قيمة الإرجاع الافتراضية هي `None`:

```py
def greet():
    print('hello') 

result = greet() # hello
print(result) # None
```

يمكنك أيضًا تزويد المعلمات بقيم افتراضية هكذا:

```py
def get_sum(num_1, num_2=2):
    return num_1 + num_2

result = get_sum(3) 
print(result) # 5
```

إذا استدعيت الدالة بدون العدد الصحيح من المعلمات، ستحصل على `TypeError`:

```py
def calculate_sum(a, b):
    print(a + b)

calculate_sum()

# TypeError: calculate_sum() missing 2 required positional arguments: 'a' and 'b'
```

## الدوال المدمجة الشائعة

- **دالة `input()`**: تُستخدم لطلب مُدخَل من المستخدم:

```py
name = input('What is your name?') # User types 'Kolade' and presses Enter  
print('Hello', name) # Hello Kolade
```

- **دالة `int()`**: تُستخدم لتحويل رقم، قيمة منطقية، أو سلسلة رقمية إلى عدد صحيح:

```py
print(int(3.14)) # 3
print(int('42')) # 42
print(int(True)) # 1
print(int(False)) # 0 
```

## النطاق في بايثون

لدى Python قواعد نطاق إضافية. ركز الآن على النطاق المحلي والعام.

- **النطاق المحلي**: المتغير الذي يُنشأ داخل دالة يمكن استخدامه فقط داخل تلك الدالة. معلمات الدالة هي أيضًا متغيرات محلية.

```py
def my_func():
    num = 10
    print(num)
```

- **النطاق العام**: المتغير الذي يُنشأ خارج الدالة يمكن استخدامه داخل الدوال وخارجها.

```py
tax = 0.70 

def get_total(subtotal):
    total = subtotal + (subtotal * tax)
    return total

print(get_total(100))  # 170.0
```

## معاملات المقارنة

- **يساوي (`==`)**: يتحقق مما إذا كانت قيمتان متساويتين:

```py
print(3 == 4) # False
```

- **لا يساوي (`!=`)**: يتحقق مما إذا كانت قيمتان غير متساويتين:

```py
print(3 != 4) # True
```

- **أكبر من بشكل صارم (`>`)**: يتحقق مما إذا كانت قيمة أكبر من أخرى:

```py
print(3 > 4) # False
```

- **أصغر من بشكل صارم (`<`)**: يتحقق مما إذا كانت قيمة أصغر من أخرى:

```py
print(3 < 4) # True
```

- **أكبر من أو يساوي (`>=`)**: يتحقق مما إذا كانت قيمة أكبر من أو تساوي أخرى:

```py
print(3 >= 4) # False
```

- **أصغر من أو يساوي (`<=`)**: يتحقق مما إذا كانت قيمة أصغر من أو تساوي أخرى:

```py
print(3 <= 4) # True
```

## العمل مع عبارات `if`، `elif` و`else`

- **عبارات `if`**: هذه شروط تُستخدم لتحديد ما إذا كان شيء ما صحيحًا أم لا. إذا قُيّم الشرط إلى `True`، فسيتم تنفيذ ذلك الجزء من الكود.

```py
age = 18

if age >= 18:
    print('You are an adult') # You are an adult
```

- **بند `elif`**: هي شروط تأتي بعد عبارة `if`. يُنفذ بند `elif` فقط إذا كانت كل الشروط السابقة `False` وشرطه الخاص `True`.

```py
age = 16

if age >= 18:
    print('You are an adult')
elif age >= 13:
    print('You are a teenager')  # You are a teenager
```

- **بند `else`**: يتم تنفيذ هذا إذا لم تُقيّم أي شروط أخرى إلى `True`.

```py
age = 12

if age >= 18:
    print('You are an adult')
elif age >= 13:
    print('You are a teenager')
else:
    print('You are a child')  # You are a child
```

يمكنك أيضًا استخدام جمل `if` متداخلة بهذا الشكل:

```py
is_citizen = True
age = 25

if is_citizen:
    if age >= 18:
        print('You are eligible to vote') # You are eligible to vote
else:
    print('You are not eligible to vote')
```

## القيم الصحيحة والخاطئة (Truthy and Falsy)

- **التعريف**: في بايثون، لكل قيمة قيمة منطقية ضمنية، أو إحساس مدمج بها إذا كان يجب التعامل معها كـ `True` أو `False` في سياق منطقي. العديد من القيم تُعتبر صحيحة (truthy)، أي تُقيّم إلى `True` في السياق المنطقي. والبعض الآخر يُعتبر خاطئًا (falsy)، أي يُقيّم إلى `False`. إليك بعض الأمثلة على القيم الخاطئة:

```md
None
False
Integer 0
Float 0.0
Empty strings ''
```

القيم الأخرى مثل الأعداد غير الصفرية والسلاسل النصية غير الفارغة تُعتبر صحيحة (truthy).

## العمل مع الدالة `bool()`

- **التعريف**: إذا أردت التحقق مما إذا كانت القيمة صحيحة أو خاطئة، يمكنك استخدام الدالة المدمجة `bool()`. تقوم بتحويل القيمة صراحةً إلى مكافئها المنطقي وتُرجع `True` للقيم الصحيحة و`False` للقيم الخاطئة. إليك بعض الأمثلة:

```py
print(bool(False)) # False
print(bool(0))  # False
print(bool('')) # False

print(bool(True)) # True
print(bool(1)) # True
print(bool('Hello')) # True
```

## عوامل القيم المنطقية والقطع المبكر (Short-circuiting)

- **التعريف**: هذه عوامل خاصة تسمح لك بدمج عدة تعبيرات لإنشاء منطق اتخاذ قرار أكثر تعقيدًا في كودك. هناك ثلاثة عوامل منطقية في Python: `and`، و`or`، و`not`.
- **عامل `and`**: يأخذ هذا العامل معاملين ويُرجع المعامل الأول إذا كان قيمة منطقية خاطئة، وإلا فإنه يُرجع المعامل الثاني. يجب أن يكون كلا المعاملين قيمتين صحيحتين لكي ينتج التعبير قيمة صحيحة.

```py
is_citizen = True
age = 25

print(is_citizen and age) # 25
```

يمكنك أيضًا استخدام عامل `and` في الشروط بهذا الشكل:

```py
is_citizen = True
age = 25

if is_citizen and age >= 18:
    print('You are eligible to vote') # You are eligible to vote
else:
    print('You are not eligible to vote')
```

- **عامل `or`**: يُرجع العامل الأول إذا كان صحيحًا، وإلا يُرجع العامل الثاني. تعبير `or` يُنتج قيمة صحيحة إذا كان أحد العاملين على الأقل صحيحًا. إليك مثالًا:

```py
age = 19
is_employed = False

print(age or is_employed) # 19
```

تمامًا كما في عامل `and`، يمكنك استخدام عامل `or` في الشروط بهذا الشكل:

```py
age = 19
is_student = True

if age < 18 or is_student:
    print('You are eligible for a student discount') # You are eligible for a student discount
else:
    print('You are not eligible for a student discount')
```

- **التوقفات (Short-circuiting)**: يُعرف عاملَي `and` و`or` بأنهما عوامل توقفات. التوقفات تعني أن بايثون يتحقق من القيم من اليسار إلى اليمين ويتوقف بمجرد أن يحدد النتيجة النهائية.
- **عامل `not`**: يأخذ هذا العامل معاملًا واحدًا ويعكس قيمته المنطقية. يحول القيم الصحيحة إلى `False` والقيم الخاطئة إلى `True`. على عكس العوامل السابقة، يعيد `not` دائمًا `True` أو `False`. إليك بعض الأمثلة:

```py
print(not '') # True, because empty string is falsy
print(not 'Hello') # False, because non-empty string is truthy
print(not 0) # True, because 0 is falsy
print(not 1) # False, because 1 is truthy
print(not False) # True, because False is falsy
print(not True) # False, because True is truthy
```

إليك مثالًا على استخدام عامل `not` في شرط:

```py
is_admin = False

if not is_admin:
    print('Access denied for non-administrators.') # Access denied for non-administrators.
else:
    print('Welcome, Administrator!')
```

# --assignment--

راجع موضوعات ومفاهيم أساسيات بايثون.
