---
id: 67fe85a1b634a335b18ae09a
title: O que é Scope em Python e como ele funciona?
challengeType: 19
dashedName: what-is-scope-in-python-and-how-does-it-work
---

# --description--

O escopo determina onde você pode usar uma variável no seu código.

Python tem regras adicionais de escopo. Por enquanto, foque no escopo local e global.

Uma variável criada fora de uma função tem escopo global. Você pode usá-la tanto dentro quanto fora das funções.

Uma variável criada dentro de uma função tem escopo local. Você só pode usá-la dentro dessa função. Parâmetros de função também são variáveis locais.

Aqui está um exemplo de escopo local e global:

```python
tax_rate = 0.1

def calculate_tax(price):
    tax = price * tax_rate
    return tax

print(calculate_tax(50)) # 5.0
print(tax_rate) # 0.1
print(tax) # NameError: name 'tax' is not defined
```

A variável `tax_rate` é global porque foi criada fora da função. A função `calculate_tax` pode lê-la, e a segunda chamada de `print()` também pode lê-la.

O parâmetro `price` e a variável `tax` são locais para `calculate_tax`. Eles estão disponíveis enquanto essa função executa, mas não fora dela. A última chamada de `print()` gera um `NameError` porque `tax` não está definido no escopo global.

# --questions--

## --text--

Onde você pode usar uma variável local?

## --answers--

Somente dentro da função onde ela foi criada.

---

Em qualquer lugar do arquivo.

### --feedback--

Uma variável criada dentro de uma função está disponível apenas nessa função.

---

Somente antes da função ser chamada.

### --feedback--

Uma variável criada dentro de uma função está disponível apenas nessa função.

---

Somente fora da função onde ela foi criada.

### --feedback--

Uma variável criada dentro de uma função está disponível apenas nessa função.

## --video-solution--

1

## --text--

Qual variável no código a seguir tem escopo global?

```python
discount = 0.2

def apply_discount(price):
    amount = price * discount
    return price - amount
```

## --answers--

`price`

### --feedback--

Procure a variável criada fora da função.

---

`amount`

### --feedback--

Procure a variável criada fora da função.

---

`discount`

---

Todas as três variáveis têm escopo global.

### --feedback--

Procure a variável criada fora da função.

## --video-solution--

3

## --text--

O que acontece quando a última linha deste código é executada?

```python
def greet():
    message = 'Hello!'
    print(message)

greet()
print(message)
```

## --answers--

Ela imprime `Hello!` pela segunda vez.

### --feedback--

A variável `message` é local para `greet`.

---

Ela imprime uma string vazia.

### --feedback--

A variável `message` é local para `greet`.

---

Ela gera um `NameError`.

---

Ela transforma `message` em uma variável global.

### --feedback--

A variável `message` é local para `greet`.

## --video-solution--

3
