---
id: 6a03f5d143c8688a9265c0a0
title: Type() 和 isinstance() 函式如何運作？
challengeType: 19
dashedName: how-do-the-type-and-isinstance-functions-work
---

# --description--

在上一課中，你學習了 Python 中常見的資料型別，包括字串、整數和浮點數型別。當你建置你的程式時，你需要學會如何查看變數的型別。

這裡有一個範例變數：

```py
developer = 'Devin'
```

要查看 `developer` 是什麼型別，你可以像這樣使用 `type()` 函式：

```py
developer = 'Devin'

print(type(developer)) # <class 'str'>
```

`<class 'str'>` 的輸出表示 `developer` 是字串型別。

如果你沒有提供任何引數給 `type()` 函式，則會收到以下錯誤訊息：

```md
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
TypeError: type() takes 1 or 3 arguments
```

以下是你目前學過的資料型別，以及 `type()` 函式對每個型別所列印的結果：

```python
my_integer_var = 10
print(type(my_integer_var))  # <class 'int'>

my_float_var = 4.50
print(type(my_float_var))  # <class 'float'>

my_string_var = 'hello'
print(type(my_string_var))  # <class 'str'>

my_boolean_var = True
print(type(my_boolean_var))  # <class 'bool'>
```

在你的程式中，有時你需要在對特定變數執行操作之前，先確認該變數是否為特定型別。這時候 `isinstance()` 函式就非常有用。

這裡有一個被指定為字串的範例變數：

```py
account_balance = '12'
```

如果你嘗試使用 `account_balance` 變數執行像除法這樣的數學運算，你將會收到錯誤訊息。

```py
account_balance = '12'

account_balance / 2

# Traceback (most recent call last):
#   File "<stdin>", line 1, in <module>
# TypeError: unsupported operand type(s) for /: 'str' and 'int'
```

要檢查 `account_balance` 是否為整數，你可以像這樣使用 `isinstance()` 函式：

```py
account_balance = '12'

isinstance(account_balance, int) # False
```

內建的 `isinstance()` 函式讓你可以檢查一個變數是否符合特定的資料型別。它接受一個值和你想檢查的型別，然後傳回一個布林值。在這個例子中，因為 `account_balance` 是字串，所以會傳回 `False`。

`isinstance()` 函式也允許你一次檢查多個型別。

這裡有一個範例，檢查 `account_balance` 是否為 `int` 或 `float`：

```py
account_balance = 12
isinstance(account_balance, (int, float)) # True
```

在這個例子中，`account_balance` 是整數，所以 `isinstance()` 傳回 `True`。如果 `account_balance` 是 `12.0`，`isinstance()` 仍會傳回 `True`，因為你正在檢查是否為整數或浮點數。

在未來的工作坊和實驗中，你會使用 `type()` 和 `isinstance()` 函式，確保你的變數在執行操作前包含正確的資料型別。

# --questions--

## --text--

以下 code 的輸出結果會是什麼？

```py
developer = 'Devin'

print(type(developer))
```

## --answers--

`<class 'int'>`

### --feedback--

回顧課程開頭以取得正確答案。

---

`<class 'str'>`

---

`<class 'float'>`

### --feedback--

回顧課程開頭以取得正確答案。

---

`<class 'bool'>`

### --feedback--

回顧課程開頭以取得正確答案。

## --video-solution--

2

## --text--

下列哪一個函式用來檢查變數是否為特定型別？

## --answers--

`isinstance()`

---

`istype()`

### --feedback--

回顧課程結尾以取得正確答案。

---

`isclass()`

### --feedback--

回顧課程結尾以取得正確答案。

---

`isinstantiate()`

### --feedback--

回顧課程結尾以取得正確答案。

## --video-solution--

1

## --text--

使用 `isinstance()` 函式時，會傳回什麼結果？

```py
account_balance = 12
isinstance(account_balance, (int, float))
```

## --answers--

`False`

### --feedback--

回顧課程結尾以取得正確答案。

---

`True`

---

`Int`

### --feedback--

回顧課程結尾以取得正確答案。

---

`12`

### --feedback--

回顧課程結尾以取得正確答案。

## --video-solution--

2
