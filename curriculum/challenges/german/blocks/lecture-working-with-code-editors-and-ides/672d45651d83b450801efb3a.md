---
id: 672d45651d83b450801efb3a
title: Wie Sie ein Projekt erstellen und Ihren Code lokal in VS Code ausführen
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

Um zu beginnen, sollten Sie wissen, dass VS Code Projekte als „Workspaces“ betrachtet. Der Workspace ist das Verzeichnis, das Sie in VS Code geöffnet haben.

Um zu beginnen, müssen Sie eine Befehlszeilenschnittstelle (CLI) verwenden, um ein neues Verzeichnis für Ihr neues Projekt zu erstellen. Ein Verzeichnis ist ein anderer Begriff für einen Ordner.

Befehlszeilenschnittstellen, kurz CLIs, ermöglichen es Ihnen, über textbasierte Befehle mit Ihrem Betriebssystem zu interagieren. Sie werden in zukünftigen Lektionen mehr über diese Werkzeuge lernen.

Wenn Sie Windows verwenden, können Sie die Eingabeaufforderung oder PowerShell nutzen. Wenn Sie einen Mac haben, öffnen Sie die Terminal-App.

Sobald die Befehlszeile geöffnet ist, navigieren Sie zu Ihrem Home-Verzeichnis. Geben Sie in der Eingabeaufforderung `cd /d %USERPROFILE%` ein und drücken Sie <kbd>Enter</kbd> auf Ihrer Tastatur. In PowerShell oder dem macOS Terminal geben Sie `cd ~` ein und drücken <kbd>Enter</kbd>. Geben Sie dann den Befehl `mkdir my-project` ein und drücken <kbd>Enter</kbd>. `mkdir`, oder „make directory“, ist der Befehl, mit dem Sie ein neues Verzeichnis erstellen. Öffnen Sie anschließend Ihr neues Verzeichnis mit VS Code.

Es gibt auch andere Möglichkeiten, ein Verzeichnis in VS Code zu öffnen.

Je nach Betriebssystem haben Sie möglicherweise ein Kontextmenü (oder eine Rechtsklick-Option), um VS Code direkt aus Ihrem Datei-Explorer zu öffnen. Sie können auch doppelt auf die VS Code-Anwendung klicken. Wenn Sie sich mit der Befehlszeile wohlfühlen, können Sie den Befehl `code /path/to/folder` ausführen.

Auf macOS ist der `code`-Befehl im Terminal erst verfügbar, wenn Sie ihn aktivieren. Öffnen Sie VS Code, drücken Sie <kbd>Cmd</kbd> + <kbd>Shift</kbd> + <kbd>P</kbd>, um die Befehlspalette zu öffnen, suchen Sie dann nach und führen Sie `Shell Command: Install 'code' command in PATH` aus. Danach funktioniert der `code`-Befehl im Terminal.

Sobald es geöffnet ist, sollten Sie einen neuen leeren Workspace sehen.

Um eine neue Datei zu erstellen, klicken Sie im Menü oben links auf `File` und wählen Sie `New File...` aus. Benennen Sie dann Ihre Datei `index.html` und klicken Sie auf `Save`.

Jetzt sollten Sie Ihre neue Datei im Explorer-Tab sehen. Versuchen Sie, etwas HTML in Ihre `index.html`-Datei einzufügen.

Vielleicht sind Sie versucht, die HTML-Datei direkt im Browser zu öffnen. Das kann zwar funktionieren, führt aber oft zu unerwarteten Bugs, wie zum Beispiel, dass CSS nicht richtig geladen wird.

Stattdessen sollten Sie Ihre HTML-Datei hinter einem richtigen Webserver ablegen. Das klingt vielleicht einschüchternd, wenn Sie neu im Programmieren sind, aber es gibt tatsächlich eine Möglichkeit, dies direkt in VS Code zu machen.

Sie müssen zuerst die Erweiterung Live Server herunterladen, die kostenlos im Marketplace verfügbar ist.

Klicken Sie auf den Erweiterungen-Tab in der linken Seitenleiste und geben Sie `Live Server` in die Suchleiste ein.

Sobald Sie diese installiert haben, sollten Sie einen `Go Live`-Button in Ihrer Statusleiste sehen. Stellen Sie sicher, dass Ihre HTML-Datei geöffnet ist, und klicken Sie dann darauf.

Ihre neue Seite sollte automatisch in einem neuen Tab im Browser geöffnet werden. Falls nicht, können Sie direkt zu `http://localhost:5500/` gehen.

Herzlichen Glückwunsch! Sie haben nun Ihr eigenes Projekt in VS Code eingerichtet und können Ihre Änderungen mit Live Server ansehen.

# --questions--

## --text--

Was betrachtet Visual Studio Code als „Workspace“?

## --answers--

Die gesamte Festplatte.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

---

Das Verzeichnis, das Sie in VS Code geöffnet haben.

---

Alle geöffneten Dateien in VS Code.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

---

Der VS Code-Anwendungsordner.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

## --video-solution--

2

## --text--

Warum empfiehlt die Lektion, einen Webserver zu verwenden, anstatt eine HTML-Datei direkt im Browser zu öffnen?

## --answers--

Es lädt schneller und erleichtert das Erkennen von Problemen im Code.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

---

Es bietet bessere Sicherheit für große Softwareanwendungen.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

---

Es vermeidet unerwartete Bugs, wie zum Beispiel, dass CSS nicht richtig geladen wird.

---

Es ermöglicht die Echtzeit-Zusammenarbeit mit anderen Entwicklern im Team.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

## --video-solution--

3

## --text--

Welche VS Code-Erweiterung wird empfohlen, um einfach einen lokalen Webserver einzurichten?

## --answers--

Web Server.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

---

Live Server.

---

Local Host.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

---

HTML Preview.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

## --video-solution--

2
