---
id: 5cc0bd7a49b71cb96132e54c
title: استخدام العودية لإنشاء نطاق من الأعداد
challengeType: 1
forumTopicId: 301180
dashedName: use-recursion-to-create-a-range-of-numbers
---

# --description--

مواصلةً للتحدٍّ السابق، نمنحك فرصة أخرى لإنشاء دالة عودية لحل مشكلة.

# --instructions--

عرّفنا دالة باسم `rangeOfNumbers` بمعلمتين. يجب أن تُرجع الدالة مصفوفة من أعداد صحيحة تبدأ برقم يمثله المعلم `startNum` وتنتهي برقم يمثله المعلم `endNum`. سيكون الرقم الابتدائي دائمًا أقل من أو يساوي الرقم النهائي. يجب أن تستخدم دالتك العودية باستدعاء نفسها دون استخدام أي حلقات. كما يجب أن تعمل في الحالات التي يكون فيها `startNum` و `endNum` متساويين.

# --hints--

يجب أن تُعيد دالتك مصفوفة.

```js
assert(Array.isArray(rangeOfNumbers(5, 10)));
```

يجب ألا يستخدم كودك أي صيغة نحوية للحلقات (`for` أو `while` أو الدوال عالية المستوى مثل `forEach`، `map`، `filter`، أو `reduce`).

```js
assert(
  !__helpers.removeJSComments(code).match(/for|while|forEach|map|filter|reduce/g)
);
```

يجب أن تستخدم `rangeOfNumbers` العودية (استدعاء نفسها) لحل هذا التحدي.

```js
assert(
  rangeOfNumbers.toString().match(/rangeOfNumbers\s*\(.+\)/)
);
```

يجب أن تُعيد `rangeOfNumbers(1, 5)` المصفوفة `[1, 2, 3, 4, 5]`.

```js
assert.deepStrictEqual(rangeOfNumbers(1, 5), [1, 2, 3, 4, 5]);
```

يجب أن تُعيد `rangeOfNumbers(6, 9)` المصفوفة `[6, 7, 8, 9]`.

```js
assert.deepStrictEqual(rangeOfNumbers(6, 9), [6, 7, 8, 9]);
```

يجب أن تُعيد `rangeOfNumbers(4, 4)` المصفوفة `[4]`.

```js
assert.deepStrictEqual(rangeOfNumbers(4, 4), [4]);
```

يجب ألا تُستخدم المتغيرات العامة لتخزين المصفوفة.

```js
rangeOfNumbers(1, 3)
assert.deepStrictEqual(rangeOfNumbers(6, 9), [6, 7, 8, 9]);
```

# --seed--

## --seed-contents--

```js
function rangeOfNumbers(startNum, endNum) {
  return [];
};
```

# --solutions--

```js
function rangeOfNumbers(startNum, endNum) {
  if (endNum - startNum === 0) {
    return [startNum];
  } else {
    const numbers = rangeOfNumbers(startNum, endNum - 1);
    numbers.push(endNum);
    return numbers;
  }
}
```
