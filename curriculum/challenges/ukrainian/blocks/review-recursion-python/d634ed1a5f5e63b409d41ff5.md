---
id: d634ed1a5f5e63b409d41ff5
title: Повторення рекурсії
challengeType: 31
dashedName: review-recursion-python
---

# --description--

- Рекурсія — це концепція програмування, яка дозволяє викликати функцію повторно, доки не буде досягнуто базового випадку.

Ось приклад рекурсивної функції, яка обчислює факторіал числа:

```py
def find_factorial(n):
    if n == 0:
        return 1
    return n * find_factorial(n - 1)
```

У наведеному вище прикладі функція `find_factorial` викликається рекурсивно, доки `n` не стане `0`. Коли `n` дорівнює `0`, досягається базовий випадок, і функція повертає `1`. Потім функція повертає добуток `n` і результату рекурсивного виклику `find_factorial(n - 1)`.

- Рекурсія дозволяє опрацьовувати структури з невідомою глибиною, наприклад, глибоко вкладені словники та списки або дерево файлів.
- Для відстеження викликів функцій у рекурсивній функції використовується стек викликів. Кожного разу, коли функція викликається, вона додається до стека викликів. Коли досягається базовий випадок, виклики функцій видаляються зі стека.
- Ви повинні ретельно визначити базовий випадок. Інакше функція може викликати себе безкінечно, доки Python не згенерує `RecursionError`.
- Рекурсія корисна для розв’язання математичних задач, таких як факторіал і Фібоначчі, обходу дерев і графів, а також для генерації перестановок і комбінацій.

# --assignment--

Повторіть теми та концепції рекурсії.
