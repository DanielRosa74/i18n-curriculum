---
id: 67168278ac6df6a799555db5
title: HTML 的 audio 和 video 元素的作用是什么，它们是如何工作的？
challengeType: 19
dashedName: what-are-the-roles-of-the-html-audio-and-video-elements
---

# --interactive--

`audio` 和 `video` 元素可让你在 HTML 文档中添加声音和视频内容。 `audio` 元素支持流行的音频格式，如 mp3、wav 和 ogg。 `video` 元素支持 mp4、ogg 和 webm 格式。

## 使用 `src` 属性添加音频

要在你的网页中包含音频内容，可以使用带有指向音频文件位置的 `src` 属性的 `audio` 元素。

正如你在预览窗口中看到的，页面上没有显示任何内容。要查看预览，你需要启用交互式编辑器。

:::interactive_editor

```html
<audio src="https://cdn.freecodecamp.org/curriculum/js-music-player/cruising-for-a-musing.mp3"></audio>
```

:::

## 使用 `controls` 属性添加播放控件

如果你想在页面上看到音频播放器，那么你可以为你的 `audio` 元素添加 `controls` 属性。

在预览窗口中按播放以听取 Quincy Larson 的一首歌曲。要听不同的歌曲，请将 `src` 值更改为 `"https://cdn.freecodecamp.org/curriculum/js-music-player/never-not-favored.mp3"`。要查看预览，你需要启用交互式编辑器。

:::interactive_editor

```html
<audio src="https://cdn.freecodecamp.org/curriculum/js-music-player/cruising-for-a-musing.mp3" controls></audio>
```

:::

`controls` 属性允许用户管理音频播放，包括暂停或恢复播放。`controls` 属性是一个布尔属性，可以添加到元素中以启用内置播放控件。如果省略，则不会显示任何控件。

**注意：** 即使存在 `controls` 属性，一些浏览器（例如 Safari）默认可能不会显示卷控件。

## 使用 `loop` 属性循环播放音频

除了 `src` 和 `controls` 属性外，还有其他几个属性可以增强 `audio` 元素的功能。 `loop` 属性是一个布尔属性，用于使音频连续重放。

这是一个使用 `loop` 属性播放 Quincy Larson 的歌曲《Can't stay down》的示例。要看到循环效果，请启用交互式编辑器，将播放头拖到歌曲接近结尾的位置，歌曲结束后会重新开始。

:::interactive_editor

```html
<audio
  src="https://cdn.freecodecamp.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
></audio>
```

:::

## 使用 `muted` 属性静音音频

你还可以使用另一个属性 `muted`。当它出现在 `audio` 元素中时，这个布尔属性会让音频以静音状态开始。下面是使用 `muted` 属性的示例。

要听音乐，请启用交互式编辑器并点击音频播放器中的卷图标。

:::interactive_editor

```html
<audio
  src="https://cdn.freecodecamp.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
  muted
></audio>
```

:::

## 支持多种音频格式

说到音频文件类型，不同的浏览器支持不同的类型。 为此，你可以在 `audio` 元素内使用 `source` 元素，浏览器将选择它能理解的第一个源。 下面是为 `audio` 元素使用多个 `source` 元素的示例：

```html
<audio controls>
  <source src="audio.ogg" type="audio/ogg" />
  <source src="audio.wav" type="audio/wav" />
  <source src="audio.mp3" type="audio/mpeg" />
</audio>
```

浏览器会首先从 ogg 类型开始，如果无法播放音频，就会下移到列表中的下一个类型。

## 使用 `video` 元素

到目前为止我们学过的所有属性也都支持在 `video` 元素中。下面是一个使用带有 `loop`、`controls` 和 `muted` 属性的 `video` 元素的示例。

为打开的 `video` 标签添加 `autoplay` 属性，使视频自动播放。要与示例交互，你需要启用交互式编辑器。

**注意：** 这里使用了 `width` 属性来缩小视频尺寸，使其更好地适应预览窗口。你将在后续课程中学习更多关于 `width` 属性的内容。

:::interactive_editor

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
  width="400"
></video>
```

:::

## 使用 `poster` 属性显示预览图像

对于 `src` 或源属性，我们使用了来自 archive.org 的一段名为“Big Buck Bunny”的视频。如果你想在视频下载时显示一张图像，可以使用 `poster` 属性。这个属性在 `audio` 元素中不可用，是 `video` 元素独有的。下面是使用 `poster` 属性的示例，内容由 peach.blender.org 提供。

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
  poster="https://peach.blender.org/wp-content/uploads/title_anouncement.jpg?x11217"
  width="400"
></video>
```

## 支持多种视频格式

你也可以在 `video` 元素内使用 `source` 元素，就像你在 `audio` 元素中所做的那样。这允许你以多种格式提供相同的视频，浏览器将选择它能播放的第一个格式。

```html
<video
  controls
  width="400"
  poster="https://peach.blender.org/wp-content/uploads/title_anouncement.jpg?x11217"
>
  <source
    src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
    type="video/mp4"
  />
  <source
    src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.webm"
    type="video/webm"
  />
  Your browser does not support the video tag.
</video>
```

# --questions--

## --text--

什么属性允许音频在静音状态下启动？

## --answers--

`mute`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`quiet`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`pause`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`muted`

## --video-solution--

4

## --text--

通过哪个属性可以看到播放和暂停按钮？

## --answers--

`loop`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

---

`controls`

---

`details`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

---

`muted`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

## --video-solution--

2

## --text--

`video` 元素中的 `poster` 属性的用途是什么？

## --answers--

设置视频源。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

---

控制视频播放。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

---

在加载视频时显示图像。

---

将视频静音。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

## --video-solution--

3
