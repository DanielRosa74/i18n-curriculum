---
id: 69babea99725b43a01adb6d1
title: Passaggio 5
challengeType: 1
dashedName: step-5
---

# --description--

Per prima cosa, devi ottenere il numero di partecipanti che arrivano durante il tick corrente. Consulta il valore nell'array `queue` del gate di input all'indice `tickIndex` e assegnalo a una variabile chiamata `currentTickQueue`.

# --hints--

Dovresti assegnare l'elemento in `gate.queue[tickIndex]` a una variabile chiamata `currentTickQueue`.

```js
const explorer = await __helpers.Explorer(code);
const { processGateFlow } = explorer.allFunctions;
const { currentTickQueue } = processGateFlow?.variables ?? {};
assert.isTrue(currentTickQueue?.value.matches("gate.queue[tickIndex]"));
```

# --seed--

## --seed-contents--

```js
const morningGates = [
  { id: "North", capacity: 5, queue: [3, 6, 2, 4] },
  { id: "East", capacity: 3, queue: [2, 4, 3, 5] },
  { id: "South", capacity: 4, queue: [1, 2, 3, 1] },
  { id: "West", capacity: 2, queue: [4, 1, 2, 3] },
];

const nightGates = [
  { id: "North", capacity: 4, queue: [6, 2, 5, 1] },
  { id: "East", capacity: 2, queue: [3, 3, 4, 2] },
  { id: "South", capacity: 5, queue: [2, 1, 2, 3] },
  { id: "West", capacity: 3, queue: [5, 2, 1, 4] },
];

function initializeThroughput(gates) {
  const summary = {};
  for (const gate of gates) {
    summary[gate.id] = 0;
  };
  return summary;
}

function processGateFlow(gate, tickIndex) {
--fcc-editable-region--
  
--fcc-editable-region--
}
```
