---
id: 687e9437536d762823fefab3
title: ¿Cuáles son algunos atajos que puedes usar en la línea de comandos para acelerar la productividad?
challengeType: 19
dashedName: what-are-some-shortcuts-you-can-use-in-the-command-line-to-speed-up-productivity
---

# --description--

Aprendamos sobre los atajos de línea de comandos que puedes usar para acelerar la productividad.

Los atajos de terminal y línea de comandos pueden ayudarte a optimizar tus flujos de trabajo y maximizar tu productividad. Para Linux y macOS, que ambos pueden rastrear sus raíces hasta Unix, muchos de estos atajos serán los mismos. Pero para Windows, habrá algunas diferencias.

El primer atajo es la tecla <kbd>Flecha arriba</kbd>. Presionarla te permite retroceder en el historial de comandos, uno a la vez. La tecla <kbd>Flecha abajo</kbd> se usa para avanzar en el historial de comandos. Estas dos teclas te permiten recorrer rápidamente los comandos que has ejecutado para encontrar uno que necesites volver a usar.

Muchos shells también ofrecen una funcionalidad de autocompletado, proponiendo comandos basados en lo que has empezado a escribir. La tecla <kbd>Tab</kbd> se usa para completar la sugerencia, llenando rápidamente la línea de comandos con la sintaxis completa. Cómo se generan estas sugerencias varía según el shell. Por ejemplo, en zsh estas sugerencias se basan en tu historial de comandos más reciente. Pero en PowerShell, se basan en comandos, variables y argumentos disponibles (con menos peso en la frecuencia de uso reciente).

A veces un comando puede generar mucha salida en tu terminal. Este ruido extra puede estorbar o interrumpir tu flujo de trabajo. En terminales basados en *nix que se encuentran en Linux y macOS, puedes presionar <kbd>Control</kbd> + <kbd>L</kbd> para limpiar toda la pantalla y empezar con un prompt limpio. En PowerShell, <kbd>Control</kbd> + <kbd>L</kbd> también limpia la pantalla por defecto, y puedes usar el comando `cls` para el mismo resultado.

Si necesitas interrumpir un comando para detener su ejecución, puedes usar <kbd>Control</kbd> + <kbd>C</kbd> para matar el proceso. Esto termina el comando y crea un nuevo prompt, permitiéndote continuar con lo que necesites hacer. Para usuarios de PowerShell, <kbd>Control</kbd> + <kbd>C</kbd> también se usa para copiar texto, y solo termina un proceso cuando el contexto no es ambiguo (por ejemplo, cuando no hay texto seleccionado para copiar).

Los siguientes atajos están disponibles solo en terminales basados en *nix, y no tienen una alternativa en PowerShell.

Puede haber momentos en que necesites multitarea, permitiendo que un proceso o comando se ejecute en segundo plano mientras trabajas en otro. Presionar <kbd>Control</kbd> + <kbd>Z</kbd> coloca el proceso actual en una tarea en segundo plano y te devuelve a la línea de comandos, donde puedes continuar tu trabajo. Cuando necesites volver a enfocarte en la tarea en segundo plano, puedes usar `fg` para restaurarla.

Y finalmente, puede haber comandos que necesites ejecutar dos veces seguidas. Puedes presionar la tecla <kbd>Flecha arriba</kbd> para retroceder al comando ejecutado previamente, pero también puedes escribir dos signos de exclamación (`!!`) en la línea de comandos y presionar <kbd>Enter</kbd>: esto ejecutará automáticamente el último comando ejecutado.

<table>
  <caption>Atajos y entradas en la línea de comandos</caption>
  <thead>
    <tr>
      <th scope="col">Acción</th>
      <th scope="col">Entrada</th>
      <th scope="col">Disponibilidad o contexto</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">Retroceder en el historial de comandos</th>
      <td><kbd>Flecha arriba</kbd></td>
      <td>Linux, macOS y PowerShell</td>
    </tr>
    <tr>
      <th scope="row">Avanzar en el historial de comandos</th>
      <td><kbd>Flecha abajo</kbd></td>
      <td>Linux, macOS y PowerShell</td>
    </tr>
    <tr>
      <th scope="row">Completar una sugerencia (autocompletado con tabulador)</th>
      <td><kbd>Tab</kbd></td>
      <td>Linux, macOS y PowerShell (la fuente de sugerencias varía según el shell: por ejemplo, zsh usa el historial reciente de comandos, mientras que PowerShell usa comandos, variables y argumentos disponibles)</td>
    </tr>
    <tr>
      <th scope="row">Limpiar el terminal</th>
      <td><kbd>Control</kbd> + <kbd>L</kbd></td>
      <td>Linux, macOS y PowerShell (el comando <code>cls</code> de PowerShell limpia la pantalla de la misma forma)</td>
    </tr>
    <tr>
      <th scope="row">Interrumpir un proceso en ejecución</th>
      <td><kbd>Control</kbd> + <kbd>C</kbd></td>
      <td>Linux, macOS y PowerShell (en PowerShell esto también copia texto, y solo termina un proceso cuando no hay nada seleccionado para copiar)</td>
    </tr>
    <tr>
      <th scope="row">Enviar un proceso a segundo plano</th>
      <td><kbd>Control</kbd> + <kbd>Z</kbd></td>
      <td>Solo Linux y macOS, sin alternativa en PowerShell</td>
    </tr>
    <tr>
      <th scope="row">Reejecutar el comando anterior</th>
      <td><code>!!</code></td>
      <td>Solo Linux y macOS, sin alternativa en PowerShell</td>
    </tr>
  </tbody>
</table>

Muchos terminales ofrecen aún más atajos, y te animo a leer la documentación de tu configuración particular para encontrar los atajos que mejor funcionen para ti. Pero estos atajos universales básicos deberían servir como un excelente punto de partida para maximizar tu productividad y convertirte en un experto en terminal.

# --questions--

## --text--

¿Qué combinación de teclas se usa para limpiar la pantalla en terminales basados en *nix?

## --answers--

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

Piensa en qué atajo crea un prompt nuevo y limpio.

---

<kbd>Control</kbd> + <kbd>L</kbd>

---

<kbd>Control</kbd> + <kbd>Z</kbd>

### --feedback--

Piensa en qué atajo crea un prompt nuevo y limpio.

---

La tecla <kbd>Tab</kbd>

### --feedback--

Piensa en qué atajo crea un prompt nuevo y limpio.

## --video-solution--

2

## --text--

¿Qué sucede cuando presionas <kbd>Control</kbd> + <kbd>Z</kbd> en un terminal basado en *nix?

## --answers--

Termina el proceso actual.

### --feedback--

Este atajo te permite realizar múltiples tareas mientras un proceso se ejecuta.

---

Coloca el proceso actual en segundo plano.

---

Limpia la pantalla.

### --feedback--

Este atajo te permite realizar múltiples tareas mientras un proceso se ejecuta.

---

Recorre el historial de comandos.

### --feedback--

Este atajo te permite realizar múltiples tareas mientras un proceso se ejecuta.

## --video-solution--

2

## --text--

¿Cuál de los siguientes se puede usar para ejecutar rápidamente el último comando ejecutado en una terminal basada en *nix?

## --answers--

<kbd>Control</kbd> + <kbd>L</kbd>

### --feedback--

Esto implica escribir un carácter específico dos veces.

---

La tecla <kbd>Tab</kbd>

### --feedback--

Esto implica escribir un carácter específico dos veces.

---

`!!`

---

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

Esto implica escribir un carácter específico dos veces.

## --video-solution--

3
