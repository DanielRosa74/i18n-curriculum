---
id: 687e9437536d762823fefab3
title: 在命令列中你可以使用哪些捷徑來加快生產力？
challengeType: 19
dashedName: what-are-some-shortcuts-you-can-use-in-the-command-line-to-speed-up-productivity
---

# --description--

讓我們來了解你可以使用的命令列捷徑，以加快生產力。

終端機和命令列捷徑可以幫助你簡化工作流程並最大化你的生產力。對於 Linux 和 macOS，兩者皆可追溯至 Unix，許多這些捷徑將會相同。但對於 Windows，則會有一些差異。

第一個捷徑是 <kbd>上箭頭</kbd> 鍵。按下這個鍵可以讓你逐一往後瀏覽你的指令歷史。接著，<kbd>下箭頭</kbd> 鍵則可以用來往前瀏覽指令歷史。這兩個鍵讓你能快速瀏覽之前執行過的指令，找到你需要再次執行的指令。

許多 shell 也會提供自動補全功能，根據你開始輸入的內容建議指令。你可以使用 <kbd>Tab</kbd> 鍵來補齊建議的剩餘部分，快速填滿命令列的完整語法。這些建議的產生方式會因 shell 而異。例如，在 zsh 中，這些建議是根據你最近的指令歷史；但在 PowerShell 中，則是根據可用的指令、變數和引數（且較少考慮最近使用的頻率）。

有時候指令會在終端機上產生大量輸出。這些額外的資訊可能會干擾或破壞你的工作流程。在 Linux 和 macOS 裝置上的 *nix 終端機中，你可以按 <kbd>Control</kbd> + <kbd>L</kbd> 來清除整個螢幕，並開始一個全新的乾淨提示字元。在 PowerShell 中，<kbd>Control</kbd> + <kbd>L</kbd> 也會預設清除螢幕，你也可以執行 `cls` 指令達到相同效果。

如果你需要中斷指令以停止其執行，可以使用 <kbd>Control</kbd> + <kbd>C</kbd> 來終止該行程。這會結束指令並產生新的提示字元，讓你繼續處理其他工作。對 PowerShell 使用者來說，<kbd>Control</kbd> + <kbd>C</kbd> 也用於複製文字，且只有在沒有選取文字可複製時才會用來終止行程。

接下來的幾個快速鍵僅在 *nix 基於終端機中可用，且沒有 PowerShell 替代方案。

有時你可能需要多工處理，讓某個行程或指令在背景執行，同時你繼續處理其他工作。按下 <kbd>Control</kbd> + <kbd>Z</kbd> 會將目前行程放到背景任務，並回到命令列，讓你繼續工作。當你需要將焦點切回背景任務時，可以使用 `fg` 指令來恢復它。

最後，有些指令你可能需要連續執行兩次。你可以按 <kbd>上箭頭</kbd> 鍵往回瀏覽到先前執行的指令，也可以在命令列輸入兩個驚嘆號（`!!`）並按 <kbd>Enter</kbd> — 這會自動執行最後一次執行的指令。

<table>
  <caption>命令列捷徑與輸入</caption>
  <thead>
    <tr>
      <th scope="col">動作</th>
      <th scope="col">輸入</th>
      <th scope="col">可用性或背景關係</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">往後瀏覽指令歷史</th>
      <td><kbd>上箭頭</kbd></td>
      <td>Linux、macOS 和 PowerShell</td>
    </tr>
    <tr>
      <th scope="row">往前瀏覽指令歷史</th>
      <td><kbd>下箭頭</kbd></td>
      <td>Linux、macOS 和 PowerShell</td>
    </tr>
    <tr>
      <th scope="row">完成建議（Tab 補全）</th>
      <td><kbd>Tab</kbd></td>
      <td>Linux、macOS 和 PowerShell（建議來源依 shell 而異 — 例如 zsh 使用最近的指令歷史，而 PowerShell 使用可用的指令、變數和引數）</td>
    </tr>
    <tr>
      <th scope="row">清除終端機</th>
      <td><kbd>Control</kbd> + <kbd>L</kbd></td>
      <td>Linux、macOS 和 PowerShell（PowerShell 的 <code>cls</code> 指令也能達到相同清除螢幕效果）</td>
    </tr>
    <tr>
      <th scope="row">中斷正在執行的行程</th>
      <td><kbd>Control</kbd> + <kbd>C</kbd></td>
      <td>Linux、macOS 和 PowerShell（在 PowerShell 中此組合鍵也用於複製文字，且只有在沒有選取文字時才會終止行程）</td>
    </tr>
    <tr>
      <th scope="row">將行程移到背景</th>
      <td><kbd>Control</kbd> + <kbd>Z</kbd></td>
      <td>僅限 Linux 和 macOS，PowerShell 無對應功能</td>
    </tr>
    <tr>
      <th scope="row">重新執行先前的指令</th>
      <td><code>!!</code></td>
      <td>僅限 Linux 和 macOS，PowerShell 無對應功能</td>
    </tr>
  </tbody>
</table>

許多終端機提供更多的捷徑，我鼓勵你閱讀你特定設定的文件，以找到最適合你的捷徑。但這些基本的通用捷徑應該能作為提升你生產力和成為終端機高手的絕佳起點。

# --questions--

## --text--

在 *nix 基於終端機中，哪個按鍵組合用於清除螢幕？

## --answers--

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

思考哪個捷徑會創建一個全新、乾淨的提示。

---

<kbd>Control</kbd> + <kbd>L</kbd>

---

<kbd>Control</kbd> + <kbd>Z</kbd>

### --feedback--

思考哪個捷徑會創建一個全新、乾淨的提示。

---

<kbd>Tab</kbd> 鍵

### --feedback--

思考哪個捷徑會創建一個全新、乾淨的提示。

## --video-solution--

2

## --text--

在 *nix 終端機中，按下 <kbd>Control</kbd> + <kbd>Z</kbd> 會發生什麼事？

## --answers--

它會終止目前的處理程序。

### --feedback--

此捷徑允許你在一個處理程序執行時同時多工處理。

---

它將目前的行程放到背景。

---

它會清除螢幕。

### --feedback--

此捷徑允許你在一個處理程序執行時同時多工處理。

---

它會循環瀏覽命令歷史記錄。

### --feedback--

此捷徑允許你在一個處理程序執行時同時多工處理。

## --video-solution--

2

## --text--

以下哪一項可用於快速執行 *nix 基於終端機的最後執行指令？

## --answers--

<kbd>Control</kbd> + <kbd>L</kbd>

### --feedback--

這涉及輸入特定字元兩次。

---

<kbd>Tab</kbd> 鍵

### --feedback--

這涉及輸入特定字元兩次。

---

`!!`

---

<kbd>Control</kbd> + <kbd>C</kbd>

### --feedback--

這涉及輸入特定字元兩次。

## --video-solution--

3
