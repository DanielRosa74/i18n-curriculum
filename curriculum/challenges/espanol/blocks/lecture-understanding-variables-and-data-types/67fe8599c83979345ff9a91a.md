---
id: 67fe8599c83979345ff9a91a
title: ¿Cómo funciona la función de impresión?
challengeType: 19
dashedName: how-does-the-print-function-work
---

# --description--

El terminal es el área donde puedes ver la salida de texto de un programa. Python incluye `print()` como una función integrada, lo que significa que puedes usarla sin definirla tú mismo. La función `print()` envía texto y otros valores al terminal. Veamos más de cerca cómo funciona.

Un programa inicial común muestra `Hello world!` en el terminal. Puedes escribir este programa en Python con la función `print`.

Para hacer eso, solo necesitas poner el string `Hello world!` entre los paréntesis de apertura y cierre que usas para llamar a la función `print`:

```python
print('Hello world!') # Hello world!
```

Aprenderás más sobre cadenas y funciones en Python en lecciones futuras. Por ahora, solo considera las cadenas como una secuencia de caracteres rodeada por comillas simples (`'`) o dobles (`"`).

En el ejemplo `print('Hello world!')`, la cadena `'Hello world!'` es un **argumento** pasado a la función `print`. También puedes usar la función `print` para mostrar múltiples valores, o argumentos, a la vez separándolos con comas. Por ejemplo:

```python
print('My favorite colors are', 'blue', 'green', 'red')

# Output: My favorite colors are blue green red
```

Python agrega automáticamente un espacio entre cada elemento cuando los separas con comas. Esto es útil cuando quieres imprimir varias piezas de información juntas.

# --questions--

## --text--

¿Por qué el siguiente código imprime todos los valores en la misma línea con espacios?

```python
print('My favorite colors are', 'blue', 'green', 'red')
```

## --answers--

Porque separar valores con comas en `print()` añade espacios entre ellos.

---

Porque las cadenas se unen automáticamente sin ningún separador.

### --feedback--

Revisa cómo Python formatea la salida cuando se pasan múltiples valores a `print()`.

---

Porque cada llamada a `print()` agrega un espacio por defecto.

### --feedback--

Revisa cómo Python formatea la salida cuando se pasan múltiples valores a `print()`.

---

Porque Python convierte automáticamente todas las cadenas a mayúsculas en la salida.

### --feedback--

Revisa cómo Python formatea la salida cuando se pasan múltiples valores a `print()`.

## --video-solution--

1

## --text--

¿Cuál sería la salida del siguiente código?

```python
print('Hello', 'world!')
```

## --answers--

`Hello world!`

---

`Helloworld!`

### --feedback--

Piensa en cómo se manejan múltiples argumentos en la función `print()`.

---

`Hello, world!`

### --feedback--

Piensa en cómo se manejan múltiples argumentos en la función `print()`.

---

`Hello\nworld!`

### --feedback--

Piensa en cómo se manejan múltiples argumentos en la función `print()`.

## --video-solution--

1

## --text--

¿Cuál es el propósito de las comillas alrededor de `Hello world!` en el siguiente código?

```python
print('Hello world!')
```

## --answers--

Definen una cadena que será impresa por la función `print()`.

---

Se usan para llamar a una función llamada `Hello world!`.

### --feedback--

Considera cómo Python identifica los valores de cadena en el código.

---

Son obligatorios solo si la cadena contiene espacios.

### --feedback--

Considera cómo Python identifica los valores de cadena en el código.

---

Le indican a Python que imprima una variable llamada `Hello world!`.

### --feedback--

Considera cómo Python identifica los valores de cadena en el código.

## --video-solution--

1
