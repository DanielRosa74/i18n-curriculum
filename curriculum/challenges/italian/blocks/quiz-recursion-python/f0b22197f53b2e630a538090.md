---
id: f0b22197f53b2e630a538090
title: Quiz sulla ricorsione
challengeType: 8
dashedName: quiz-recursion-python
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 9 delle 10 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è la ricorsione nella programmazione?

#### --distractors--

Un metodo per ordinare le liste.

---

Un loop che non finisce mai.

---

Una funzione che restituisce `None`.

#### --answer--

Un processo in cui una funzione chiama se stessa.

### --question--

#### --text--

Quale dei seguenti è un esempio di ricorsione?

#### --distractors--

```py
def factorial(n):
    result = 1
    while n > 0:
        result *= n
        n -= 1
    return result
```

---

```py
def factorial(n):
    numbers = [number for number in range(1, n + 1)]
    result = 1
    for number in numbers:
        result *= number
    return result
```

---

```py
def factorial(n):
    result = 1
    for number in range(n, 0, -1):
        result *= number
    return result
```

#### --answer--

```py
def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)
```

### --question--

#### --text--

Cosa restituirà la seguente funzione?

```py
def sum_numbers(n):
    if n == 0:
        return 0
    return n + sum_numbers(n - 1)

sum_numbers(3)
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

Quante volte verrà chiamata la funzione `mystery`?

```py
def mystery(n):
    if n <= 1:
        return 1
    return mystery(n - 2)

mystery(5)
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

Quale delle seguenti affermazioni è vera riguardo alla ricorsione?

#### --distractors--

Non dovrebbe mai avere un caso base.

---

Dovrebbe essere usata solo per la sequenza di Fibonacci.

---

Dovrebbe essere usata solo per i fattoriali.

#### --answer--

Dovrebbe sempre avere un caso base.

### --question--

#### --text--

Quale delle seguenti opzioni sarebbe un caso base appropriato per l'esempio dato?

```py
def count_down_to_zero(number):
    # Base case goes here.

    print(number)
    count_down_to_zero(number - 1)
```

#### --distractors--

```py
if number > 0:
    return
```

---

```py
if number != 0:
    return
```

---

```py
if number == 0:
    return
```

#### --answer--

```py
if number < 0:
    return
```

### --question--

#### --text--

Perché la ricorsione deve avere un caso base?

#### --distractors--

Per assicurare che la funzione abbia sempre un valore di ritorno.

---

Per ridurre il numero di chiamate di funzione.

---

Per assicurarsi che tutte le chiamate ricorsive vengano eseguite nel corretto ordine.

#### --answer--

Per fornire un modo alla funzione di smettere di fare chiamate ricorsive ed evitare un `RecursionError`.

### --question--

#### --text--

Cosa farà questa funzione ricorsiva?

```py
def repeat_string(string):
    return string + repeat_string(string)
```

#### --distractors--

Restituirà la stringa due volte.

---

Restituisce `None`.

---

Creerà una stringa vuota.

#### --answer--

Genera un `RecursionError`.

### --question--

#### --text--

Cos'è uno stack di chiamate?

#### --distractors--

Una lista di chiamate di funzione che sono state eseguite.

---

Una lista di valori restituiti da una funzione.

---

Una funzione speciale usata per chiamare una funzione esattamente tre volte.

#### --answer--

Una struttura dati che tiene traccia delle chiamate di funzione e del loro ordine di esecuzione.

### --question--

#### --text--

Cosa fa questa funzione ricorsiva?

```py
def change_string(string):
    if string == "":
        return ""
    return change_string(string[1:]) + string[0]
```

#### --distractors--

Duplica una stringa.

---

Rimuove le vocali.

---

Rimuove gli spazi.

#### --answer--

Inverte una stringa.
