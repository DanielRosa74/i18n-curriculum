---
id: 672d456f4ac35950b300e93f
title: 在 VS Code 中，有哪些有用的键盘快捷键能最大化工作效率？
challengeType: 19
dashedName: what-are-several-useful-keyboard-shortcuts-for-maximizing-productivity-in-vs-code
---

# --description--

你可能已经熟悉了一些基本快捷方式，它们都是从操作系统中继承下来的。 例如，在 VS Code 中，`Ctrl + S` 保存文件、`Ctrl + C` 复制内容、`Ctrl + V` 粘贴内容，这些快捷键都可以正常使用。

但有一些快捷键是应用特定的，仍然可以提升你的生产力。值得注意的是，这些快捷键在不同操作系统上可能有所不同。

例如，`Shift + Alt + F` 会在当前打开的文件上运行你配置的格式化工具（比如针对 JavaScript 项目的 Prettier）。

使用 `Ctrl + Shift + F`（Windows/Linux）或 `Cmd + Shift + F`（macOS）可以搜索工作区内所有文件的文本内容。如果你想执行查找并替换，可以使用 `Ctrl + Shift + H`（Windows/Linux）或 `Cmd + Shift + H`（macOS）。

如果你需要删除一行，`Ctrl + Shift + K`（Windows/Linux）或 `Cmd + Shift + K`（macOS）可以删除该行。

需要为你的代码腾出更多空间吗？`Ctrl + B`（Windows）或 `Cmd + B`（Mac）会隐藏侧边栏，侧边栏包含文件列表和扩展（程序、功能）菜单。

或者你看不清代码？`Ctrl + Plus`（Windows）或 `Cmd + Plus`（Mac）会放大编辑器的缩放比例，`Ctrl + Minus`（Windows）或 `Cmd + Minus`（Mac）会缩小它。

VS Code 另一个有用的特色是多行编辑，它允许你一次性修改多行内容。例如，你可以使用 `Ctrl + Alt + Down` 和 `Ctrl + Alt + Up`（Windows/Linux），或 `Option + Cmd + Down` 和 `Option + Cmd + Up`（Mac）在连续行上放置多个光标。你也可以按住 `Alt`（Windows/Linux）或 `Option`（Mac）并点击，在需要的不同行上添加光标。光标放置好后，你输入或删除的内容会同时应用到所有这些位置——这在处理重复代码时能节省大量时间。

最后，如果你忘记了任何快捷键，总可以使用 `Ctrl + Shift + P`（Windows/Linux）或 `Cmd + Shift + P`（macOS）打开命令面板，选择你需要的任何命令。

掌握这些知识，再加上一些练习，你离成为 VS Code 高级用户就不远了。

# --questions--

## --text--

在 VS Code 中，哪个 Windows 键盘快捷键用于对当前打开的文件运行已配置的格式化程序？

## --answers--

`Ctrl + F`

### --feedback--

课程提到了用于格式化当前 file 的特定快捷。

---

`Shift + Alt + F`

---

`Ctrl + Shift + F`

### --feedback--

课程提到了用于格式化当前 file 的特定快捷。

---

`Alt + F4`

### --feedback--

课程提到了用于格式化当前 file 的特定快捷。

## --video-solution--

2

## --text--

在 VS Code 中，键盘快捷键 `Ctrl + Shift + K`（Windows/Linux）或 `Cmd + Shift + K`（macOS）执行什么函数？

## --answers--

打开命令面板。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

---

增大编辑器的文字大小。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

---

删除当前行。

---

隐藏侧边栏。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

## --video-solution--

3

## --text--

哪个 Windows 键盘快捷打开 VS Code 中的命令面板？

## --answers--

`Ctrl + P`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

---

`Ctrl + Shift + P`

---

`Alt + F4`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

---

`Ctrl + Space`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

## --video-solution--

2
