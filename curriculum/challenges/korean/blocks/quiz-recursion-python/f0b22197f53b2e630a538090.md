---
id: f0b22197f53b2e630a538090
title: 재귀 퀴즈
challengeType: 8
dashedName: quiz-recursion-python
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

프로그래밍에서 재귀란 무엇인가요?

#### --distractors--

리스트를 정렬하는 방법입니다.

---

끝나지 않는 반복문입니다.

---

`None`을 반환하는 함수입니다.

#### --answer--

함수가 자기 자신을 호출하는 과정입니다.

### --question--

#### --text--

다음 중 재귀의 예는 무엇인가요?

#### --distractors--

```py
def factorial(n):
    result = 1
    while n > 0:
        result *= n
        n -= 1
    return result
```

---

```py
def factorial(n):
    numbers = [number for number in range(1, n + 1)]
    result = 1
    for number in numbers:
        result *= number
    return result
```

---

```py
def factorial(n):
    result = 1
    for number in range(n, 0, -1):
        result *= number
    return result
```

#### --answer--

```py
def factorial(n):
    if n == 0 or n == 1:
        return 1
    return n * factorial(n - 1)
```

### --question--

#### --text--

다음 함수는 무엇을 반환할까요?

```py
def sum_numbers(n):
    if n == 0:
        return 0
    return n + sum_numbers(n - 1)

sum_numbers(3)
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

`mystery` 함수가 몇 번 호출될까요?

```py
def mystery(n):
    if n <= 1:
        return 1
    return mystery(n - 2)

mystery(5)
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

재귀에 대해 옳은 설명은 무엇인가요?

#### --distractors--

기본 사례(base case)가 없어야 합니다.

---

피보나치 수열에만 사용해야 합니다.

---

팩토리얼에만 사용해야 합니다.

#### --answer--

항상 기본 사례가 있어야 합니다.

### --question--

#### --text--

주어진 예제에 적절한 기본 사례는 무엇인가요?

```py
def count_down_to_zero(number):
    # Base case goes here.

    print(number)
    count_down_to_zero(number - 1)
```

#### --distractors--

```py
if number > 0:
    return
```

---

```py
if number != 0:
    return
```

---

```py
if number == 0:
    return
```

#### --answer--

```py
if number < 0:
    return
```

### --question--

#### --text--

왜 재귀에는 기본 사례가 반드시 있어야 하나요?

#### --distractors--

함수가 항상 반환값을 갖도록 하기 위해서입니다.

---

함수 호출 횟수를 줄이기 위해서입니다.

---

모든 재귀 호출이 올바른 순서로 실행되도록 보장하기 위해서입니다.

#### --answer--

함수가 재귀 호출을 멈추고 `RecursionError`를 피할 수 있도록 하기 위해서입니다.

### --question--

#### --text--

이 재귀 함수는 무엇을 할까요?

```py
def repeat_string(string):
    return string + repeat_string(string)
```

#### --distractors--

문자열을 두 번 반환합니다.

---

`None`을 반환합니다.

---

빈 문자열을 만듭니다.

#### --answer--

`RecursionError`를 발생시킵니다.

### --question--

#### --text--

호출 스택이란 무엇인가요?

#### --distractors--

실행된 함수 호출 목록입니다.

---

함수가 반환한 값들의 목록입니다.

---

함수를 정확히 세 번 호출하는 특수 함수입니다.

#### --answer--

함수 호출과 실행 순서를 추적하는 자료 구조입니다.

### --question--

#### --text--

이 재귀 함수는 무엇을 하나요?

```py
def change_string(string):
    if string == "":
        return ""
    return change_string(string[1:]) + string[0]
```

#### --distractors--

문자열을 복제합니다.

---

모음을 제거합니다.

---

공백을 제거합니다.

#### --answer--

문자열을 뒤집습니다.
