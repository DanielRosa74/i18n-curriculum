---
id: d634ed1a5f5e63b409d41ff5
title: Revisão de Recursão
challengeType: 31
dashedName: review-recursion-python
---

# --description--

- Recursão é um conceito de programação que permite chamar uma função repetidamente até que um caso base seja alcançado.

Aqui está um exemplo de uma função recursiva que calcula o fatorial de um número:

```py
def find_factorial(n):
    if n == 0:
        return 1
    return n * find_factorial(n - 1)
```

No exemplo acima, a função `find_factorial` é chamada recursivamente até que `n` chegue a `0`. Quando `n` é `0`, o caso base é alcançado e a função retorna `1`. Em seguida, a função retorna o produto de `n` pelo resultado da chamada recursiva para `find_factorial(n - 1)`.

- Recursão permite lidar com algo de profundidade desconhecida, como dicionários e listas profundamente aninhados, ou uma árvore de arquivos.
- Uma pilha de chamadas é usada para controlar as chamadas de função em uma função recursiva. Cada vez que uma função é chamada, ela é adicionada à pilha de chamadas. Quando o caso base é alcançado, as chamadas de função são removidas da pilha.
- Você deve definir cuidadosamente o caso base. Caso contrário, a função pode continuar se chamando até que o Python lance um `RecursionError`.
- Recursão é útil para resolver problemas matemáticos como fatorial e Fibonacci, percorrer árvores e grafos, e gerar permutações e combinações.

# --assignment--

Revise os tópicos e conceitos de Recursion.
