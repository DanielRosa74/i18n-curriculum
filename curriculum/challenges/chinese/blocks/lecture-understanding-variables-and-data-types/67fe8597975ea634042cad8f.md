---
id: 67fe8597975ea634042cad8f
title: 如何声明变量以及命名变量的命名约定是什么？
challengeType: 19
dashedName: how-do-you-declare-variables-and-what-are-naming-conventions-to-name-variables
---

# --description--

在 Python 中，变量就像带标签的盒子，用于保存和参考不同类型的数据。要创建一个变量，在左边写变量名，接着是赋值操作符（`=`），右边写你想保存的值。下面是如何创建 `name` 和 `age` 变量的示例：

```python
name = 'John Doe'
age = 25
```

在上面的例子中，变量 `name` 保存了值 `'John Doe'`。该值是一个字串，字串是一串用于表现文本的字符。字串用单引号或双引号书写，例如 `'Hello'` 或 `"Hello"`。在后续课程中，你将学习更多关于在 Python 中操作字串的内容。

在 Python 中命名变量时，有一些重要规则你应该牢记：

- 变量名只能以字母或下划线（`_`）开头，不能以数字开头。
- 变量名只能包含字母数字字符（`a-z`、`A-Z`、`0-9`）和下划线（`_`）。
- 变量名区分大小写：`age`、`Age` 和 `AGE` 都被视为不同的变量。
- 变量名不能是 Python 的保留关键字，比如 `if`、`class` 或 `def`。

如果你违反了这些规则，Python 程序会引起一个 `SyntaxError`：

```md
    5variable_name = 5
     ^
SyntaxError: invalid syntax
```

现在让我们来了解一些 Python 中变量的常见命名约定。

首先，变量名应该全部小写，单词之间用下划线分隔。这种命名方式叫做蛇形命名法（snake case）：

```python
my_variable_name = 'freeCodeCamp'
```

接下来，你应该为变量使用描述性名称。例如，如果你想将用户的年龄储存为变量，`user_age` 比 `age` 或缩略语如 `ua` 更好：

```python
user_age = 30
```

这样，你可以在大型代码库中轻松地向其他团队成员（或你未来的自己）传达一个变量的用途。

另一个约定是避免使用单个字母作为变量名。虽然单字母变量名在 Python 中很常见，但应避免使用，因为它们无法传达变量的用途或含义：

```python
x = 56 # What do you mean by x?
```

井号符号（`#`）及其后面的文本构成评论。评论让你可以为代码添加注释和解释。

在 Python 中，评论以井号符号（`#`）开头，语言会忽略该行中 `#` 符号之后的所有内容：

```python
# This is a single-line comment
```

多行评论可以通过连续的单行评论来创建：

```python
# This is a
# multi-line
# comment
```

你可以使用评论来解释你的代码，为自己留下提醒，或说明某行代码存在的原因。当你学习或团队协作时，评论尤其有用。

但是，你不应该使用评论来解释你的变量名的含义。相反，你为变量选择的名称应该具有描述性，能够传达它们的用途，并遵循前面提到的其他命名规则以防止语法误差。

# --questions--

## --text--

以下哪种方式是用 Python 声明变量的正确方法？

## --answers--

`25 = age`

### --feedback--

先写变量名，然后是赋值操作符，最后是值。

---

`age 25`

### --feedback--

先写变量名，然后是赋值操作符，最后是值。

---

`= age 25`

### --feedback--

先写变量名，然后是赋值操作符，最后是值。

---

`age = 25`

## --video-solution--

4

## --text--

以下哪项**不是**命名变量的规则？

## --answers--

变量名必须以字母或下划线开头。

### --feedback--

Python 将大写字母和小写字母视为不同。

---

变量名不区分分支。

---

变量名不能使用 Python 保留关键字。

### --feedback--

Python 将大写字母和小写字母视为不同。

---

变量名可以包含字母、数字和下划线。

### --feedback--

Python 将大写字母和小写字母视为不同。

## --video-solution--

2

## --text--

为什么你应该避免使用单字母变量名？

## --answers--

它们没有传达变量的目的。

---

Python 不允许使用单字母变量名。

### --feedback--

有意义的变量名提高大型代码库的可读性。

---

它们增加了语法错误的风险。

### --feedback--

有意义的变量名提高大型代码库的可读性。

---

它们比更长的变量名使用更多内存。

### --feedback--

有意义的变量名提高大型代码库的可读性。

## --video-solution--

1
